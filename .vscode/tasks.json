{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "echo Hello"
        },
        {
            "label": "Filter Trading Logs",
            "type": "shell",
            "command": "grep -E 'app=\\s*trading_operation=[^\\s]+' logs.txt",
            "problemMatcher": []
        }
    ]
}