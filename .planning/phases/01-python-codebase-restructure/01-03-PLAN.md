---
phase: 01-python-codebase-restructure
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/clients/python/demos/demo_covered_call.py
  - src/clients/python/demos/demo_wheel_strategy.py
  - src/clients/python/demos/demo_pdf_wheel_strategy.py
  - src/clients/python/demos/demo_mean_reversion.py
  - src/clients/python/demos/demo_options_mean_reversion.py
  - src/clients/python/demos/demo_credit_spread.py
  - src/clients/python/engine/trading_engine_optimizer.py
  - src/clients/python/engine/trading_engine.py
  - src/clients/python/tests/test_demo_covered_call.py
  - src/clients/python/tests/test_credit_spread_strategy.py
  - src/clients/python/tests/test_mean_reversion_strategy.py
  - src/clients/python/tests/test_options_mean_reversion_strategy.py
  - src/clients/python/tests/test_pdf_wheel_strategy.py
  - src/clients/python/tests/test_deviation_levels.py
  - src/clients/python/tests/test_kelly_sizing.py
  - src/clients/python/tests/test_partial_exit_manager.py
  - src/clients/python/tests/test_pdf_builder.py
  - src/clients/python/tests/test_return_models.py
  - src/clients/python/tests/test_mean_reversion_report.py
autonomous: true
requirements: [CONS-03, CONS-04]

must_haves:
  truths:
    - "All 6 demo scripts use engine.trading_engine.run_strategy() instead of their own tick loops"
    - "trading_engine_optimizer.py works with the new engine and strategy interface (D-06)"
    - "All 11 existing test files pass (tests collect and run without failures)"
    - "Running python -m demos.demo_covered_call --help from src/clients/python/ works"
  artifacts:
    - path: "src/clients/python/demos/demo_covered_call.py"
      provides: "Covered call demo using run_strategy()"
      contains: "run_strategy"
    - path: "src/clients/python/demos/demo_mean_reversion.py"
      provides: "Mean reversion demo using run_strategy()"
      contains: "run_strategy"
    - path: "src/clients/python/engine/trading_engine_optimizer.py"
      provides: "Bayesian optimizer using new strategy interface"
      contains: "run_strategy"
  key_links:
    - from: "src/clients/python/demos/demo_covered_call.py"
      to: "src/clients/python/engine/trading_engine.py"
      via: "from engine.trading_engine import run_strategy"
      pattern: "from engine\\.trading_engine import run_strategy"
    - from: "src/clients/python/engine/trading_engine_optimizer.py"
      to: "src/clients/python/engine/trading_engine.py"
      via: "from engine.trading_engine import run_strategy"
      pattern: "from engine\\.trading_engine import run_strategy"
    - from: "src/clients/python/tests/test_demo_covered_call.py"
      to: "src/clients/python/demos/demo_covered_call.py"
      via: "subprocess python -m demos.demo_covered_call"
      pattern: "demos\\.demo_covered_call"
---

<objective>
Refactor all 6 demo scripts to use trading_engine.run_strategy(), refactor the optimizer to use the new interface (D-06), and validate that all existing tests pass after the full restructure.

Purpose: Complete the strategy consolidation by wiring demos through the unified engine and ensuring backward compatibility via the test suite. This is the final phase plan -- after this, CONS-03 and CONS-04 are satisfied.
Output: 6 refactored demo scripts, refactored optimizer, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-python-codebase-restructure/01-CONTEXT.md
@.planning/phases/01-python-codebase-restructure/01-RESEARCH.md
@.planning/phases/01-python-codebase-restructure/01-01-SUMMARY.md
@.planning/phases/01-python-codebase-restructure/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor all 6 demo scripts to use run_strategy()</name>
  <files>
    src/clients/python/demos/demo_covered_call.py
    src/clients/python/demos/demo_wheel_strategy.py
    src/clients/python/demos/demo_pdf_wheel_strategy.py
    src/clients/python/demos/demo_mean_reversion.py
    src/clients/python/demos/demo_options_mean_reversion.py
    src/clients/python/demos/demo_credit_spread.py
  </files>
  <read_first>
    src/clients/python/demos/demo_covered_call.py
    src/clients/python/demos/demo_wheel_strategy.py
    src/clients/python/demos/demo_pdf_wheel_strategy.py
    src/clients/python/demos/demo_mean_reversion.py
    src/clients/python/demos/demo_options_mean_reversion.py
    src/clients/python/demos/demo_credit_spread.py
    src/clients/python/engine/trading_engine.py
    src/clients/python/strategies/base_strategy.py
    src/clients/python/strategies/covered_call.py
    src/clients/python/strategies/mean_reversion.py
  </read_first>
  <action>
    Each demo script currently has its own strategy loop (calling per-strategy `run_*()` functions). Refactor each to use `engine.trading_engine.run_strategy()`.

    **Pattern for each demo** (per CONS-03):
    ```python
    from engine.trading_engine import run_strategy
    from engine.client import BacktesterPlaygroundClient
    from strategies.<name> import <StrategyClass>

    def main():
        # ... argparse setup stays the same ...
        # ... playground creation stays the same ...
        strategy = StrategyClass(playground, symbol, logger, **strategy_kwargs)
        run_strategy(strategy, playground, logger)
        # ... print results / cleanup stays the same ...
    ```

    **For each demo, read the current code carefully to understand:**
    1. What arguments the strategy constructor needs (beyond playground, symbol, logger)
    2. What the current `run_*()` function does before/after the tick loop (setup, cleanup, printing)
    3. What output/results are printed at the end

    Keep ALL pre-loop setup (argparse, playground creation, PDF building, etc.) in the demo.
    Keep ALL post-loop output (results printing, playground cleanup) in the demo.
    Replace ONLY the middle section (the tick loop call) with `run_strategy()`.

    **Demo-specific notes:**

    - **demo_covered_call.py**: Currently calls a function like `run_options_strategy()` from options_strategy_basic_v7 (now strategies/covered_call). Replace with `run_strategy(strategy, playground, logger)`. The strategy constructor may need parameters from argparse (max_open_count, etc.).

    - **demo_wheel_strategy.py**: Similar to covered_call but uses WheelStrategy.

    - **demo_pdf_wheel_strategy.py**: Builds PDF first, then creates playground, then runs. Keep PDF building in demo, pass PDF to strategy constructor.

    - **demo_mean_reversion.py**: Builds PDF, creates playground, runs strategy. Keep PDF building, pass to constructor.

    - **demo_options_mean_reversion.py**: Same pattern as mean_reversion.

    - **demo_credit_spread.py**: Same pattern as mean_reversion.

    **CRITICAL for demo_covered_call.py:** This demo is tested by `tests/test_demo_covered_call.py` which runs it as a subprocess and checks specific output patterns (Final timestamp, Final balance, etc.). The output format MUST remain identical. Ensure the same print statements exist in the same format.

    **sys.path handling:** Each demo needs `src/clients/python/` on sys.path to resolve absolute imports. Add at the top of each demo if not already present:
    ```python
    import sys, os
    sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
    ```
    This makes `from engine.X import Y` work when running as `python demos/demo_covered_call.py` or `python -m demos.demo_covered_call`.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m demos.demo_covered_call --help 2>&1 | head -5</automated>
  </verify>
  <done>
    - All 6 demo scripts import and call run_strategy() from engine.trading_engine
    - All 6 demo scripts create their strategy instance and pass it to run_strategy()
    - No demo script has its own tick loop anymore
    - demo_covered_call.py preserves exact output format for test_demo_covered_call.py compatibility
    - Each demo can be invoked with --help without errors
  </done>
  <acceptance_criteria>
    - `grep "from engine.trading_engine import run_strategy" src/clients/python/demos/demo_covered_call.py` matches
    - `grep "from engine.trading_engine import run_strategy" src/clients/python/demos/demo_mean_reversion.py` matches
    - `grep "from engine.trading_engine import run_strategy" src/clients/python/demos/demo_credit_spread.py` matches
    - `grep "from engine.trading_engine import run_strategy" src/clients/python/demos/demo_wheel_strategy.py` matches
    - `grep "from engine.trading_engine import run_strategy" src/clients/python/demos/demo_pdf_wheel_strategy.py` matches
    - `grep "from engine.trading_engine import run_strategy" src/clients/python/demos/demo_options_mean_reversion.py` matches
    - `grep "run_strategy(" src/clients/python/demos/demo_covered_call.py` matches
    - Running `python -m demos.demo_covered_call --help` from src/clients/python/ does not error
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Refactor trading_engine_optimizer.py for new interface (D-06)</name>
  <files>
    src/clients/python/engine/trading_engine_optimizer.py
    src/clients/python/engine/trading_engine.py
  </files>
  <read_first>
    src/clients/python/engine/trading_engine_optimizer.py
    src/clients/python/engine/trading_engine.py
    src/clients/python/strategies/base_strategy.py
  </read_first>
  <action>
    Refactor `trading_engine_optimizer.py` to use the new engine and strategy interface (per D-06).

    **Read the current optimizer to understand:**
    1. How it imports `objective` from `trading_engine`
    2. What `objective()` does (creates playground, builds strategy, runs it, returns metrics)
    3. How `gp_minimize` from skopt calls the objective function
    4. What hyperparameters are tuned

    **Refactoring approach** (per D-07 -- Claude's discretion on optimizer integration pattern):

    Option A (recommended -- strategy factory pattern):
    - Refactor `objective()` in engine/trading_engine.py to accept a strategy factory callable
    - The factory receives hyperparameters and returns a configured strategy instance
    - The objective function creates a playground, calls the factory, runs `run_strategy()`, returns metrics
    - The optimizer defines the factory and passes it to objective()
    - Call `run_strategy()` with `enable_retraining=False` during optimization (D-05)

    Option B (simpler -- keep objective self-contained):
    - Keep `objective()` mostly as-is but update it to use `run_strategy()` internally
    - The objective function still knows how to create the strategy
    - Just update imports and the tick loop call

    Choose whichever is cleaner after reading the actual code. The key constraint is:
    - `enable_retraining=False` MUST be used during optimizer runs (D-05)
    - The optimizer must work with skopt's `gp_minimize` pattern
    - The refactoring happens in THIS phase, not deferred (D-06)
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -c "
from engine.trading_engine_optimizer import *
print('Optimizer imports OK')
"</automated>
  </verify>
  <done>
    - trading_engine_optimizer.py uses the new strategy interface
    - It calls run_strategy() with enable_retraining=False (D-05)
    - It works with skopt's gp_minimize pattern
    - It imports cleanly without errors
  </done>
  <acceptance_criteria>
    - `grep "run_strategy" src/clients/python/engine/trading_engine_optimizer.py` matches OR `grep "run_strategy" src/clients/python/engine/trading_engine.py` matches in the objective function
    - `grep "enable_retraining.*False\|enable_retraining=False" src/clients/python/engine/trading_engine_optimizer.py src/clients/python/engine/trading_engine.py` matches at least once
    - `grep "gp_minimize\|from skopt" src/clients/python/engine/trading_engine_optimizer.py` matches (skopt integration preserved)
    - The python verification command prints "Optimizer imports OK"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Validate all tests pass and fix any remaining import issues</name>
  <files>
    src/clients/python/tests/test_credit_spread_strategy.py
    src/clients/python/tests/test_mean_reversion_strategy.py
    src/clients/python/tests/test_options_mean_reversion_strategy.py
    src/clients/python/tests/test_pdf_wheel_strategy.py
    src/clients/python/tests/test_deviation_levels.py
    src/clients/python/tests/test_kelly_sizing.py
    src/clients/python/tests/test_partial_exit_manager.py
    src/clients/python/tests/test_pdf_builder.py
    src/clients/python/tests/test_return_models.py
    src/clients/python/tests/test_mean_reversion_report.py
  </files>
  <read_first>
    src/clients/python/tests/test_credit_spread_strategy.py
    src/clients/python/tests/test_mean_reversion_strategy.py
    src/clients/python/tests/test_options_mean_reversion_strategy.py
    src/clients/python/tests/test_pdf_wheel_strategy.py
    src/clients/python/tests/test_deviation_levels.py
    src/clients/python/tests/test_kelly_sizing.py
    src/clients/python/tests/test_partial_exit_manager.py
    src/clients/python/tests/test_pdf_builder.py
    src/clients/python/tests/test_return_models.py
    src/clients/python/tests/test_mean_reversion_report.py
    src/clients/python/conftest.py
  </read_first>
  <action>
    1. Run the full test suite to identify any remaining failures:
       ```bash
       cd src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/ -v --ignore=tests/test_demo_covered_call.py 2>&1
       ```
       (Ignore test_demo_covered_call.py because it requires a running Go server)

    2. For each failing test:
       - Read the test file
       - Identify the import error or assertion failure
       - Fix the import to use the new absolute path (from engine.X, from lib.X, from strategies.X)
       - If the test creates strategy instances directly, ensure the constructor call is compatible with the new BaseStrategy __init__

    3. Common fixes expected:
       - Tests importing `from mean_reversion_strategy import ...` need `from strategies.mean_reversion import ...`
       - Tests importing `from credit_spread_strategy import ...` need `from strategies.credit_spread import ...`
       - Tests importing `from pdf_builder import ...` need `from lib.pdf_builder import ...`
       - Tests importing `from deviation_levels import ...` need `from lib.deviation_levels import ...`
       - Tests importing `from partial_exit_manager import ...` need `from lib.partial_exit_manager import ...`
       - Tests importing `from risk_management import ...` need `from lib.risk_management import ...`
       - Tests importing `from return_models import ...` need `from lib.return_models import ...`

    4. After fixing all imports, run the suite again to confirm all pass:
       ```bash
       cd src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/ -v --ignore=tests/test_demo_covered_call.py 2>&1
       ```

    5. Also run a pytest collection check to verify all tests are discoverable:
       ```bash
       cd src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/ --co -q 2>&1
       ```

    NOTE: test_demo_covered_call.py requires a running Go server and takes 10-15 minutes. Do NOT run it. Only verify it can be collected (import check) and that its subprocess invocation path is correct (verified in Plan 01).

    IMPORTANT (CONS-04): The requirement says "All existing strategy tests pass without modification to test logic (only import paths change)." If any test fails for reasons OTHER than import paths, investigate but do NOT change test assertions or logic. Report the failure as a concern.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/ -v --ignore=tests/test_demo_covered_call.py 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All test files in tests/ can be collected by pytest (no import errors on collection)
    - All tests pass (excluding test_demo_covered_call.py which needs a live server)
    - Only import paths were changed in test files, not test logic (CONS-04)
    - test_demo_covered_call.py subprocess path updated to use python -m demos.demo_covered_call
  </done>
  <acceptance_criteria>
    - `/Users/jamal/miniconda3/envs/grodt/bin/python -m pytest src/clients/python/tests/ --co -q --ignore=src/clients/python/tests/test_demo_covered_call.py 2>&1 | grep "test"` shows collected tests
    - `/Users/jamal/miniconda3/envs/grodt/bin/python -m pytest src/clients/python/tests/ -v --ignore=src/clients/python/tests/test_demo_covered_call.py 2>&1 | grep -c "PASSED"` returns count > 0
    - `/Users/jamal/miniconda3/envs/grodt/bin/python -m pytest src/clients/python/tests/ -v --ignore=src/clients/python/tests/test_demo_covered_call.py 2>&1 | grep "FAILED"` returns no matches (0 failures)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. All 6 demos use run_strategy() from engine.trading_engine
2. Optimizer uses new interface with enable_retraining=False
3. `python -m demos.demo_covered_call --help` works from src/clients/python/
4. `pytest tests/ -v --ignore=tests/test_demo_covered_call.py` -- all pass
5. `pytest tests/ --co -q` -- all 11 test files collected
</verification>

<success_criteria>
- All 6 demo scripts call engine.trading_engine.run_strategy() (CONS-03)
- trading_engine_optimizer.py refactored for new interface (D-06) with enable_retraining=False (D-05)
- All existing tests pass with only import path changes (CONS-04)
- No Python files in flat root except __init__.py, conftest.py, utils.py (verified from Plan 01)
- The full phase success criteria from ROADMAP are met:
  1. python -m demos.demo_covered_call works end-to-end (structure-wise, needs live server)
  2. All 6 strategies launch via trading_engine.run_strategy()
  3. All existing strategy tests pass
  4. No .py files remain in flat root
</success_criteria>

<output>
After completion, create `.planning/phases/01-python-codebase-restructure/01-03-SUMMARY.md`
</output>
