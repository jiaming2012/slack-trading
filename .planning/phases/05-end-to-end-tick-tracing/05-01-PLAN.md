---
phase: 05-end-to-end-tick-tracing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/clients/python/engine/client.py
  - src/go/backtester-api/rpc/twirp.go
  - src/clients/python/tests/test_trace_propagation.py
autonomous: true
requirements: [TICK-01, TICK-02, TICK-03, TICK-04]

must_haves:
  truths:
    - "Python RPC calls inject W3C traceparent HTTP header when an OTel span is active"
    - "Python RPC calls inject no traceparent header when no span is active (backtest mode)"
    - "Go Twirp handler extracts traceparent from incoming HTTP headers and creates child spans"
    - "Go Twirp handler still recovers from panics (panicRecoveryMiddleware preserved inside otelhttp)"
  artifacts:
    - path: "src/clients/python/engine/client.py"
      provides: "traceparent injection via opentelemetry.propagate.inject()"
      contains: "inject(headers)"
    - path: "src/go/backtester-api/rpc/twirp.go"
      provides: "otelhttp middleware wrapping Twirp handler"
      contains: "otelhttp.NewHandler"
    - path: "src/clients/python/tests/test_trace_propagation.py"
      provides: "Unit tests for traceparent injection behavior"
      contains: "traceparent"
  key_links:
    - from: "src/clients/python/engine/client.py"
      to: "src/go/backtester-api/rpc/twirp.go"
      via: "W3C traceparent HTTP header on Twirp requests"
      pattern: "inject\\(headers\\)"
    - from: "src/go/backtester-api/rpc/twirp.go"
      to: "src/go/backtester-api/router/grpc.go"
      via: "otelhttp extracts traceparent into context, handlers call trace.SpanFromContext(ctx)"
      pattern: "otelhttp\\.NewHandler"
---

<objective>
Wire W3C traceparent HTTP header propagation between Python Twirp client and Go Twirp server so that a single trace in Grafana/Tempo shows the complete path from Python tick loop through Go RPC handlers.

Purpose: Enables the operator to see distributed traces spanning both Python strategy client and Go server as a single connected trace tree in Tempo, completing the observability pipeline.
Output: Modified client.py with header injection, modified twirp.go with otelhttp middleware, and test coverage for injection behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-end-to-end-tick-tracing/05-CONTEXT.md
@.planning/phases/05-end-to-end-tick-tracing/05-RESEARCH.md

@src/clients/python/engine/client.py
@src/go/backtester-api/rpc/twirp.go

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/clients/python/engine/client.py (line 153-160):
```python
def _network_call_with_retry_inner(self, caller, client, request, backoff, max_backoff):
    retries = 0
    while True:
        try:
            response = client(
                ctx=Context(),  # <-- injection point: replace with Context(headers=headers)
                request=request
            )
```

From src/go/backtester-api/rpc/twirp.go (line 34-35):
```go
mux := http.NewServeMux()
mux.Handle(twirpHandler.PathPrefix(), panicRecoveryMiddleware(twirpHandler))
// ^-- wrap with otelhttp.NewHandler(panicRecoveryMiddleware(twirpHandler), "twirp")
```

From twirp/context.py (twirpy library):
```python
class Context:
    def __init__(self, headers=None, ...):
        # headers dict is merged into HTTP request headers
```

From opentelemetry.propagate:
```python
def inject(carrier, context=None, setter=None):
    # Writes traceparent + tracestate to carrier dict if span is active
    # No-op if no active span
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Python traceparent injection test and implementation</name>
  <files>src/clients/python/tests/test_trace_propagation.py, src/clients/python/engine/client.py</files>
  <read_first>
    - src/clients/python/engine/client.py (lines 153-160 for _network_call_with_retry_inner)
    - src/clients/python/tests/ (list directory for existing test patterns)
  </read_first>
  <behavior>
    - Test 1: When an OTel span is active, _network_call_with_retry_inner passes a Context with "traceparent" in its headers to the client callable
    - Test 2: When no OTel span is active, _network_call_with_retry_inner passes a Context with NO "traceparent" in its headers (inject is a no-op)
    - Test 3: The traceparent header format is valid W3C: "00-{32hex}-{16hex}-{2hex}"
  </behavior>
  <action>
    **Test file** (`src/clients/python/tests/test_trace_propagation.py`):
    Create a test file with these tests using unittest and the OTel SDK test utilities:
    1. Import `TracerProvider` from `opentelemetry.sdk.trace`, `trace` from `opentelemetry`, `inject` from `opentelemetry.propagate`
    2. Create a mock client callable that captures the `ctx` argument passed to it
    3. Create a minimal BacktesterPlaygroundClient-like test that calls `_network_call_with_retry_inner` directly (or test the injection logic in isolation)
    4. For test with active span: set up a TracerProvider, start a span, call the method, assert `ctx.get_headers()` contains "traceparent" matching pattern `00-[0-9a-f]{32}-[0-9a-f]{16}-[0-9a-f]{2}`
    5. For test without active span: call without any span context, assert "traceparent" is NOT in headers

    **Implementation** (`src/clients/python/engine/client.py`):
    Per D-01, add W3C traceparent injection in `_network_call_with_retry_inner()`:
    1. Add `from opentelemetry.propagate import inject` to imports (line 17 area)
    2. In `_network_call_with_retry_inner()`, before the `client()` call:
       - Create `headers = {}` dict
       - Call `inject(headers)` -- writes traceparent when span is active, no-op otherwise
       - Pass `ctx=Context(headers=headers)` instead of `ctx=Context()`
    3. Per D-03, do NOT remove the existing `trace_id=_get_trace_id()` calls in `tick()` and `place_order()` -- both proto trace_id and HTTP traceparent coexist

    This is approximately 3 lines of change in one method. The `inject()` call is safe in all modes -- no conditional logic needed per research Pitfall 1.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/test_trace_propagation.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "from opentelemetry.propagate import inject" src/clients/python/engine/client.py
    - grep -q "inject(headers)" src/clients/python/engine/client.py
    - grep -q "Context(headers=headers)" src/clients/python/engine/client.py
    - grep -q "traceparent" src/clients/python/tests/test_trace_propagation.py
    - test file contains at least 2 test methods
  </acceptance_criteria>
  <done>
    - _network_call_with_retry_inner injects traceparent via opentelemetry.propagate.inject() into Context headers
    - Unit tests verify traceparent is present when span active, absent when no span
    - Existing proto trace_id field unchanged (D-03)
    - All existing Python tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Go otelhttp middleware on Twirp handler</name>
  <files>src/go/backtester-api/rpc/twirp.go</files>
  <read_first>
    - src/go/backtester-api/rpc/twirp.go (full file, it is short)
    - src/go/utils/otel.go (confirm W3C propagator is set globally)
  </read_first>
  <action>
    Per D-02, wrap the Twirp HTTP handler with `otelhttp.NewHandler()` to extract W3C traceparent from incoming requests.

    In `src/go/backtester-api/rpc/twirp.go`:
    1. Add import: `"go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp"` (already in go.mod v0.52.0)
    2. Change line 35 from:
       `mux.Handle(twirpHandler.PathPrefix(), panicRecoveryMiddleware(twirpHandler))`
       to:
       `mux.Handle(twirpHandler.PathPrefix(), otelhttp.NewHandler(panicRecoveryMiddleware(twirpHandler), "twirp"))`

    Per research Pitfall 2: otelhttp MUST be the outermost wrapper (outside panicRecoveryMiddleware). This ensures trace context is extracted from HTTP headers before any handler code runs, and panics are still caught by the inner middleware.

    Per research Pitfall 3: The existing `trace.SpanFromContext(ctx)` calls in grpc.go will now receive the span that otelhttp created. They enrich it with attributes (playground_id, symbol, etc.) rather than creating duplicate spans. No changes to grpc.go needed.

    After modification, verify Go builds cleanly.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading && go build ./src/go/backtester-api/rpc/</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "otelhttp" src/go/backtester-api/rpc/twirp.go
    - grep -q 'otelhttp.NewHandler' src/go/backtester-api/rpc/twirp.go
    - grep -q 'panicRecoveryMiddleware' src/go/backtester-api/rpc/twirp.go (preserved inside otelhttp)
    - go build ./src/go/backtester-api/rpc/ succeeds
  </acceptance_criteria>
  <done>
    - Twirp HTTP handler is wrapped with otelhttp.NewHandler as outermost middleware
    - panicRecoveryMiddleware preserved as inner middleware
    - Go package builds without errors
    - Incoming requests with traceparent header will automatically create child spans linked to the Python parent
  </done>
</task>

</tasks>

<verification>
1. Python tests pass: `cd src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/test_trace_propagation.py -x -v`
2. Go builds cleanly: `go build ./src/go/...`
3. Existing Go tests pass: `cd /Users/jamal/projects/slack-trading && go test ./src/go/backtester-api/... -count=1`
4. grep confirms injection: `grep -n "inject(headers)" src/clients/python/engine/client.py`
5. grep confirms middleware: `grep -n "otelhttp.NewHandler" src/go/backtester-api/rpc/twirp.go`
</verification>

<success_criteria>
- Python `_network_call_with_retry_inner()` injects W3C traceparent HTTP header on every RPC call when a span is active
- Go Twirp handler extracts traceparent from incoming HTTP headers via otelhttp middleware
- Panic recovery middleware is preserved (inside otelhttp wrapper)
- Proto trace_id field unchanged on both NextTickRequest and PlaceOrderRequest (D-03)
- Unit tests verify injection behavior (with and without active span)
- Both Go and Python codebases build/test cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-end-to-end-tick-tracing/05-01-SUMMARY.md`
</output>
