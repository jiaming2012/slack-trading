---
phase: 25-complete-strategy-migrations
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - src/clients/python/engine/client.py
  - src/clients/python/engine/trading_engine.py
  - src/clients/python/deprecated/covered_call.py
  - src/clients/python/deprecated/wheel.py
  - src/clients/python/deprecated/pdf_wheel.py
  - src/clients/python/deprecated/mean_reversion.py
  - src/clients/python/deprecated/options_mean_reversion.py
  - src/clients/python/deprecated/credit_spread.py
autonomous: true
requirements: [MIG-01, MIG-02]

must_haves:
  truths:
    - "client.py tick() extracts new_signals from TickDelta and calls strategy.on_signal() for each"
    - "All 6 V1 strategy files are moved to deprecated/"
    - "No import errors when running with V2 strategies only"
  artifacts:
    - path: "src/clients/python/engine/client.py"
      provides: "on_signal() dispatch from TickDelta.new_signals"
      contains: "on_signal"
    - path: "src/clients/python/deprecated/covered_call.py"
      provides: "Archived V1 CoveredCall"
    - path: "src/clients/python/deprecated/wheel.py"
      provides: "Archived V1 Wheel"
    - path: "src/clients/python/deprecated/pdf_wheel.py"
      provides: "Archived V1 PDFWheel"
    - path: "src/clients/python/deprecated/mean_reversion.py"
      provides: "Archived V1 MeanReversion"
    - path: "src/clients/python/deprecated/options_mean_reversion.py"
      provides: "Archived V1 OptionsMeanReversion"
    - path: "src/clients/python/deprecated/credit_spread.py"
      provides: "Archived V1 CreditSpread"
  key_links:
    - from: "src/clients/python/engine/client.py"
      to: "src/clients/python/strategies/base_strategy.py"
      via: "strategy.on_signal(signal) callback stored during tick"
      pattern: "new_signals"
---

<objective>
Wire on_signal() delivery in client.py tick(), move all V1 strategy files to deprecated/, and verify imports work with V2-only strategies per D-03, D-04, D-05.

Purpose: Completes MIG-01 (all strategies migrated) and MIG-02 (originals deprecated). After this plan, the codebase uses only V2 strategies with the TradeSignal framework consumption path wired.
Output: Updated client.py with on_signal() wiring, all V1 files in deprecated/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-complete-strategy-migrations/25-CONTEXT.md
@.planning/phases/25-complete-strategy-migrations/25-01-SUMMARY.md
@.planning/phases/25-complete-strategy-migrations/25-02-SUMMARY.md

<interfaces>
From src/clients/python/engine/client.py tick() method (~line 563):
```python
def tick(self, seconds: int, raise_exception=True, fetch_account: bool = True):
    new_state: TickDelta = self.network_call_with_retry('tick', self.client.NextTick, request)
    new_candles = new_state.new_candles
    # ... processes candles, timestamp, account ...
    self._new_state_buffer.append(new_state)
```

From src/clients/python/strategies/base_strategy.py:
```python
class BaseStrategy(ABC):
    def on_signal(self, signal) -> None:
        """Handle a TradeSignal from TickDelta.new_signals. Default: no-op."""
        pass
```

From rpc/playground_pb2.py TickDelta message:
```
TickDelta has field: new_signals (repeated TradeSignalProto)
```

V1 files to move to deprecated/:
- strategies/mean_reversion.py
- strategies/options_mean_reversion.py
- strategies/credit_spread.py
- strategies/covered_call.py
- strategies/wheel.py
- strategies/pdf_wheel.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire on_signal() in client.py tick()</name>
  <files>
    src/clients/python/engine/client.py
  </files>
  <read_first>
    src/clients/python/engine/client.py
    src/clients/python/strategies/base_strategy.py
    src/clients/python/rpc/playground_pb2.py
    src/clients/python/engine/trading_engine.py
  </read_first>
  <action>
    Per D-03: Wire on_signal() delivery in client.py tick() method.

    In client.py `tick()` method (around line 588-620), after processing new_candles and before appending to _new_state_buffer:

    1. Extract new_signals from TickDelta response: `new_signals = new_state.new_signals`
    2. Store signals in a buffer accessible to the strategy (same pattern as _new_state_buffer)
    3. Add a `_signal_callback` attribute (default None) that the trading engine can set to `strategy.on_signal`
    4. If `_signal_callback` is set and new_signals exist, call `self._signal_callback(signal)` for each signal

    The trading engine (trading_engine.py run_strategy()) should set `playground._signal_callback = strategy.on_signal` before the tick loop starts. This keeps client.py decoupled from strategy type.

    IMPORTANT: The on_signal() call happens DURING tick processing (not after flush), because signals are time-synchronized with the tick that delivered them.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -c "from engine.client import BacktesterPlaygroundClient; print('import OK')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - client.py tick() contains `new_signals` extraction from TickDelta
    - client.py has `_signal_callback` attribute initialized to None in __init__
    - client.py tick() calls `self._signal_callback(signal)` for each new signal when callback is set
    - trading_engine.py run_strategy() sets `playground._signal_callback = strategy.on_signal`
    - No import errors
  </acceptance_criteria>
  <done>client.py tick() extracts new_signals from TickDelta and dispatches to strategy.on_signal() via callback; trading engine wires the callback</done>
</task>

<task type="auto">
  <name>Task 2: Move V1 strategies to deprecated/ and verify V2-only imports</name>
  <files>
    src/clients/python/deprecated/covered_call.py
    src/clients/python/deprecated/wheel.py
    src/clients/python/deprecated/pdf_wheel.py
    src/clients/python/deprecated/mean_reversion.py
    src/clients/python/deprecated/options_mean_reversion.py
    src/clients/python/deprecated/credit_spread.py
    src/clients/python/strategies/covered_call.py
    src/clients/python/strategies/wheel.py
    src/clients/python/strategies/pdf_wheel.py
  </files>
  <read_first>
    src/clients/python/strategies/covered_call.py
    src/clients/python/strategies/wheel.py
    src/clients/python/strategies/pdf_wheel.py
    src/clients/python/strategies/mean_reversion.py
    src/clients/python/strategies/options_mean_reversion.py
    src/clients/python/strategies/credit_spread.py
    src/clients/python/strategies/covered_call_v2.py
    src/clients/python/strategies/wheel_v2.py
    src/clients/python/strategies/pdf_wheel_v2.py
    src/clients/python/deprecated/__init__.py
  </read_first>
  <action>
    Per D-04: Move ALL V1 originals to deprecated/.

    1. Use `git mv` to move these 6 files from strategies/ to deprecated/:
       - strategies/mean_reversion.py -> deprecated/mean_reversion.py
       - strategies/options_mean_reversion.py -> deprecated/options_mean_reversion.py
       - strategies/credit_spread.py -> deprecated/credit_spread.py
       - strategies/covered_call.py -> deprecated/covered_call.py
       - strategies/wheel.py -> deprecated/wheel.py
       - strategies/pdf_wheel.py -> deprecated/pdf_wheel.py

    2. CRITICAL: V2 strategies import shared types FROM V1 files (e.g., `from strategies.covered_call import OptionContract, CloseSignalV2, ...`). After moving V1 to deprecated/, update these imports in ALL V2 files:
       - covered_call_v2.py: change `from strategies.covered_call import ...` to `from deprecated.covered_call import ...`
       - wheel_v2.py: change `from strategies.wheel import ...` to `from deprecated.wheel import ...`
       - pdf_wheel_v2.py: change `from strategies.pdf_wheel import ...` to `from deprecated.pdf_wheel import ...`
       - Also check credit_spread_v2.py, mean_reversion_v2.py, options_mean_reversion_v2.py for V1 imports
       - Also check diff test files for V1 imports and update them

    3. Update any other files that import from the moved V1 locations:
       - Check demos/, tests/, engine/ for imports from `strategies.covered_call`, `strategies.wheel`, `strategies.pdf_wheel`, `strategies.mean_reversion`, `strategies.options_mean_reversion`, `strategies.credit_spread`
       - Update to `deprecated.*` imports

    4. Verify no import errors: run `python -c "from strategies.covered_call_v2 import OptionsStrategyBasicV2; from strategies.wheel_v2 import WheelStrategyV2; from strategies.pdf_wheel_v2 import PDFWheelStrategyV2; print('OK')"`

    Per D-05: The trading engine should be able to run with only V2 strategies after this.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -c "from strategies.covered_call_v2 import OptionsStrategyBasicV2; from strategies.wheel_v2 import WheelStrategyV2; from strategies.pdf_wheel_v2 import PDFWheelStrategyV2; from strategies.credit_spread_v2 import CreditSpreadStrategyV2; from strategies.mean_reversion_v2 import MeanReversionStrategyV2; from strategies.options_mean_reversion_v2 import OptionsMeanReversionStrategyV2; print('All V2 imports OK')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `strategies/covered_call.py` no longer exists (moved to deprecated/)
    - `strategies/wheel.py` no longer exists (moved to deprecated/)
    - `strategies/pdf_wheel.py` no longer exists (moved to deprecated/)
    - `strategies/mean_reversion.py` no longer exists (moved to deprecated/)
    - `strategies/options_mean_reversion.py` no longer exists (moved to deprecated/)
    - `strategies/credit_spread.py` no longer exists (moved to deprecated/)
    - All 6 V1 files exist in deprecated/
    - All 6 V2 strategy imports succeed without errors
    - All diff tests still pass after import path updates
  </acceptance_criteria>
  <done>All V1 strategy files moved to deprecated/; all V2 strategies import cleanly; no broken imports anywhere in the codebase</done>
</task>

</tasks>

<verification>
- client.py tick() dispatches new_signals to strategy.on_signal()
- All 6 V1 files in deprecated/, none in strategies/
- All V2 imports work
- All existing diff tests still pass after import updates
</verification>

<success_criteria>
- on_signal() wired in client.py tick() with callback pattern
- Trading engine sets callback before tick loop
- All V1 files moved to deprecated/
- All V2 imports succeed
- Diff tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-complete-strategy-migrations/25-03-SUMMARY.md`
</output>
