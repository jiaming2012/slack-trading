---
phase: 02-go-otel-foundation-local-backend
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/go/playground.proto
  - src/go/playground/playground.pb.go
  - src/go/playground/playground.twirp.go
  - src/clients/python/rpc/playground_pb2.py
  - src/clients/python/rpc/playground_twirp.py
  - src/go/backtester-api/router/grpc.go
autonomous: true
requirements: [OTEL-04, OTEL-05]

must_haves:
  truths:
    - "NextTickRequest proto message has a trace_id string field"
    - "PlaceOrderRequest proto message has a trace_id string field"
    - "Go and Python protobuf stubs are regenerated and reflect the new fields"
    - "Go NextTick handler extracts trace_id from request and attaches it to log fields"
    - "Go PlaceOrder handler extracts trace_id from request and attaches it to log fields"
    - "Server compiles and all existing tests pass after proto changes"
  artifacts:
    - path: "src/go/playground.proto"
      provides: "Proto definition with trace_id fields on NextTickRequest and PlaceOrderRequest"
      contains: "trace_id"
    - path: "src/go/playground/playground.pb.go"
      provides: "Regenerated Go protobuf stubs"
      contains: "TraceId"
    - path: "src/clients/python/rpc/playground_pb2.py"
      provides: "Regenerated Python protobuf stubs"
      contains: "trace_id"
    - path: "src/go/backtester-api/router/grpc.go"
      provides: "Handlers with trace_id extraction and structured logging"
      contains: "TraceId"
  key_links:
    - from: "src/go/playground.proto"
      to: "src/go/playground/playground.pb.go"
      via: "protoc code generation (task gen:proto)"
      pattern: "trace_id"
    - from: "src/go/backtester-api/router/grpc.go"
      to: "req.TraceId"
      via: "Handler reads trace_id from protobuf request"
      pattern: "req\\.TraceId"
---

<objective>
Add trace_id fields to NextTickRequest and PlaceOrderRequest in playground.proto, regenerate Go and Python stubs, and update Go RPC handlers to extract and use trace_id in structured log fields and span attributes. This enables end-to-end trace correlation between Python client and Go server.

Purpose: Per D-10 through D-14, every tick and order must carry a trace_id so that signals, errors, and order events can be correlated back to the originating trace. The Python client will set trace_id from its active OTel span (Phase 4), but the proto fields and Go-side extraction must exist first.
Output: Updated proto, regenerated stubs (Go + Python), updated Go handlers with trace_id logging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-go-otel-foundation-local-backend/02-CONTEXT.md
@.planning/phases/02-go-otel-foundation-local-backend/02-RESEARCH.md

<interfaces>
<!-- Proto message structures that will be modified -->

From src/go/playground.proto:
```protobuf
message NextTickRequest {
    string playground_id = 1;
    uint64 seconds = 2;
    bool is_preview = 3;
    string request_id = 4;
    // ADD: string trace_id = 5;  (per D-10)
}

message PlaceOrderRequest {
    string playground_id = 1;
    string symbol = 2;
    string asset_class = 3;
    double quantity = 4;
    string side = 5;
    string type = 6;
    string duration = 7;
    string tag = 8;
    double requested_price = 9;
    optional double price = 10;
    optional uint64 close_order_id = 11;
    bool is_adjustment = 12;
    optional string client_request_id = 13;
    optional double sl = 14;
    map<string, string> attributes = 15;
    // ADD: string trace_id = 16;  (per D-12)
}
```

From src/go/backtester-api/router/grpc.go (handler signatures):
```go
func (s *Server) NextTick(ctx context.Context, req *pb.NextTickRequest) (*pb.TickDelta, error)
func (s *Server) PlaceOrder(ctx context.Context, req *pb.PlaceOrderRequest) (*pb.Order, error)
```

From taskfile.yml (proto generation command):
```yaml
gen:proto:
    cmds:
      - 'source ${PROJECT_DIR}/src/clients/python/env/bin/activate'
      - 'protoc --proto_path=${PROJECT_DIR}/src/go --go_out=${PROJECT_DIR} --python_out=${PROJECT_DIR}/src/clients/python --twirp_out=${PROJECT_DIR} --twirpy_out=${PROJECT_DIR}/src/clients/python ${PROJECT_DIR}/src/go/playground.proto'
      - 'mv ${PROJECT_DIR}/playground/playground.pb.go ${PROJECT_DIR}/src/go/playground'
      - 'mv ${PROJECT_DIR}/playground/playground.twirp.go ${PROJECT_DIR}/src/go/playground'
      - 'rm -rf ${PROJECT_DIR}/playground'
      - 'mv ${PROJECT_DIR}/src/clients/python/playground_pb2.py ${PROJECT_DIR}/src/clients/python/rpc'
      - 'mv ${PROJECT_DIR}/src/clients/python/playground_twirp.py ${PROJECT_DIR}/src/clients/python/rpc'
```

From src/go/backtester-api/router/grpc.go (logrus import convention):
```go
import (
    log "github.com/sirupsen/logrus"
    pb "github.com/jiaming2012/slack-trading/src/go/playground"
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add trace_id fields to proto and regenerate stubs</name>
  <files>src/go/playground.proto, src/go/playground/playground.pb.go, src/go/playground/playground.twirp.go, src/clients/python/rpc/playground_pb2.py, src/clients/python/rpc/playground_twirp.py</files>
  <read_first>
    - src/go/playground.proto (full file -- need exact field numbers)
    - taskfile.yml (gen:proto task for regeneration command)
  </read_first>
  <action>
    **Modify `src/go/playground.proto`** per D-10 and D-12:

    1. Add `string trace_id = 5;` to `NextTickRequest` (after `request_id = 4`). Per D-10: "Every tick must have a trace_id."

    2. Add `string trace_id = 16;` to `PlaceOrderRequest` (after `attributes = 15`). Per D-12: "The trace_id must appear on any order."

    Both fields are plain `string` (not `optional`) because per D-10 "every tick must have a trace_id". An empty string is acceptable as a zero-value for backward compatibility with existing callers that don't set it yet (Python client will set it in Phase 4 per D-14).

    **Regenerate protobuf stubs** per D-13. Run the gen:proto task commands directly (since `task gen:proto` may require a Python venv that isn't available). Run these commands sequentially:

    ```bash
    cd /Users/jamal/projects/slack-trading
    protoc --proto_path=src/go --go_out=. --python_out=src/clients/python --twirp_out=. --twirpy_out=src/clients/python src/go/playground.proto
    mv playground/playground.pb.go src/go/playground/
    mv playground/playground.twirp.go src/go/playground/
    rm -rf playground
    mv src/clients/python/playground_pb2.py src/clients/python/rpc/
    mv src/clients/python/playground_twirp.py src/clients/python/rpc/
    ```

    If `protoc` is not found or twirp/twirpy plugins are missing, use `task gen:proto` instead (it activates the Python venv first). If the venv at `src/clients/python/env` does not exist, try using the conda env: activate grodt conda env before running protoc.

    **Verify regenerated stubs contain the new fields:**
    - `src/go/playground/playground.pb.go` should contain `TraceId string` in both `NextTickRequest` and `PlaceOrderRequest` structs
    - `src/clients/python/rpc/playground_pb2.py` should reference `trace_id`
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading && grep -q 'trace_id' src/go/playground.proto && grep -q 'TraceId' src/go/playground/playground.pb.go && grep -q 'trace_id' src/clients/python/rpc/playground_pb2.py && go build ./src/go/... && echo "proto + stubs OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'string trace_id = 5' src/go/playground.proto
    - grep -q 'string trace_id = 16' src/go/playground.proto
    - grep -q 'TraceId' src/go/playground/playground.pb.go
    - grep -q 'trace_id' src/clients/python/rpc/playground_pb2.py
    - go build ./src/go/... succeeds
  </acceptance_criteria>
  <done>Proto has trace_id on NextTickRequest (field 5) and PlaceOrderRequest (field 16). Go and Python stubs regenerated. Go packages compile.</done>
</task>

<task type="auto">
  <name>Task 2: Update Go handlers to extract trace_id and attach to logs and spans</name>
  <files>src/go/backtester-api/router/grpc.go</files>
  <read_first>
    - src/go/backtester-api/router/grpc.go (lines 685-730 for NextTick handler, lines 1070-1120 for PlaceOrder handler)
    - src/go/utils/otel.go (created in 02-01, for OTel import patterns)
  </read_first>
  <action>
    **Update `NextTick` handler** (around line 690) per D-11 and D-14:

    1. After the existing `log.Tracef` calls at the top of the function, add structured log field with trace_id:
       ```go
       logger := log.WithFields(log.Fields{
           "trace_id":      req.TraceId,
           "playground_id": req.PlaygroundId,
           "request_id":    req.RequestId,
       })
       ```
    2. Replace the existing `log.Tracef("%v: NextTick:start", req.RequestId)` with `logger.Trace("NextTick:start")` and similarly for the deferred end log.
    3. Replace `log.Debugf("Returning cached data for request id: %s", req.RequestId)` with `logger.Debug("Returning cached data")`.
    4. Replace `log.Errorf("failed to store tick delta: %v", err)` in the defer with `logger.WithError(err).Error("failed to store tick delta")`.
    5. If trace_id is non-empty, add it as a span attribute on any active span in the context:
       ```go
       if req.TraceId != "" {
           span := trace.SpanFromContext(ctx)
           span.SetAttributes(attribute.String("trace_id", req.TraceId))
       }
       ```
       Import `"go.opentelemetry.io/otel/trace"` and `"go.opentelemetry.io/otel/attribute"` at the top of the file.

    **Update `PlaceOrder` handler** (around line 1075) per D-11 and D-14:

    1. After parsing playground_id, create a structured logger:
       ```go
       logger := log.WithFields(log.Fields{
           "trace_id":      req.TraceId,
           "playground_id": req.PlaygroundId,
           "symbol":        req.Symbol,
           "side":          req.Side,
       })
       ```
    2. Add trace_id as span attribute (same pattern as NextTick).
    3. Add a `logger.Info("PlaceOrder:start")` at the top of the handler after the logger creation.

    **Field naming convention** per D-05: use snake_case (`trace_id`, `playground_id`, `request_id`, `symbol`, `side`). Per D-11: trace_id must appear on signals/errors.

    **Do NOT change the function signatures** -- only add logging/tracing within existing handler bodies. Existing behavior must be preserved.

    **Add imports** at the top of grpc.go if not already present:
    - `"go.opentelemetry.io/otel/attribute"`
    - `"go.opentelemetry.io/otel/trace"`
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading && grep -q 'req.TraceId' src/go/backtester-api/router/grpc.go && grep -q '"trace_id"' src/go/backtester-api/router/grpc.go && go build ./src/go/... && go test -count=1 ./src/go/backtester-api/... && echo "handlers updated OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'req.TraceId' src/go/backtester-api/router/grpc.go
    - grep -q '"trace_id"' src/go/backtester-api/router/grpc.go
    - grep -q 'attribute.String.*trace_id' src/go/backtester-api/router/grpc.go
    - grep -q 'trace.SpanFromContext' src/go/backtester-api/router/grpc.go
    - grep -q '"playground_id"' src/go/backtester-api/router/grpc.go
    - go build ./src/go/... succeeds
    - go test -count=1 ./src/go/backtester-api/... passes
  </acceptance_criteria>
  <done>NextTick and PlaceOrder handlers extract trace_id from requests, attach it to structured log fields (snake_case per D-05) and span attributes (per D-11). Server compiles and existing tests pass.</done>
</task>

</tasks>

<verification>
- `grep -c 'trace_id' src/go/playground.proto` returns 2 (one in NextTickRequest, one in PlaceOrderRequest)
- `grep -q 'TraceId' src/go/playground/playground.pb.go` confirms Go stubs regenerated
- `grep -q 'trace_id' src/clients/python/rpc/playground_pb2.py` confirms Python stubs regenerated
- `grep -q 'req.TraceId' src/go/backtester-api/router/grpc.go` confirms handlers use the field
- `go build ./src/go/...` succeeds (all Go packages compile)
- `go test -count=1 ./src/go/backtester-api/...` passes (no regressions)
</verification>

<success_criteria>
- Proto has trace_id on NextTickRequest (field 5) and PlaceOrderRequest (field 16)
- Go and Python protobuf stubs are regenerated with trace_id fields
- NextTick handler logs trace_id, playground_id, request_id as structured fields
- PlaceOrder handler logs trace_id, playground_id, symbol, side as structured fields
- Both handlers attach trace_id as span attribute when non-empty
- Server compiles and all existing unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-go-otel-foundation-local-backend/02-03-SUMMARY.md`
</output>
