---
phase: 04-python-telemetry-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/clients/python/requirements.txt
  - grodt.yml
  - src/clients/python/engine/otel.py
  - src/clients/python/engine/heartbeat.py
  - src/clients/python/tests/test_otel.py
  - src/clients/python/tests/test_heartbeat.py
autonomous: true
requirements: [PYTEL-01, PYTEL-02, BEAT-03, BEAT-04]

must_haves:
  truths:
    - "OTel Python SDK is importable in grodt conda env without breaking numpy 1.26.4"
    - "setup_otel() creates TracerProvider and MeterProvider with OTLP HTTP exporters"
    - "setup_otel() is idempotent (safe to call multiple times from optimizer runs)"
    - "Python strategy heartbeat gauge updates every 30 seconds in a background daemon thread"
    - "Heartbeat structured log includes strategy_name, state, tick_count, last_tick_time, uptime_seconds"
  artifacts:
    - path: "src/clients/python/engine/otel.py"
      provides: "OTel SDK initialization"
      exports: ["setup_otel"]
    - path: "src/clients/python/engine/heartbeat.py"
      provides: "Strategy heartbeat daemon thread"
      exports: ["StrategyHeartbeat"]
    - path: "src/clients/python/tests/test_otel.py"
      provides: "Tests for OTel setup"
    - path: "src/clients/python/tests/test_heartbeat.py"
      provides: "Tests for heartbeat"
  key_links:
    - from: "src/clients/python/engine/otel.py"
      to: "opentelemetry SDK"
      via: "TracerProvider + MeterProvider initialization"
      pattern: "trace\\.set_tracer_provider|metrics\\.set_meter_provider"
    - from: "src/clients/python/engine/heartbeat.py"
      to: "opentelemetry metrics"
      via: "gauge metric creation"
      pattern: "create_gauge.*grodt\\.strategy\\.heartbeat"
---

<objective>
Install OTel Python SDK and create the foundation modules: engine/otel.py (setup_otel function) and engine/heartbeat.py (StrategyHeartbeat daemon thread).

Purpose: Establishes the Python telemetry foundation that all subsequent instrumentation depends on (span creation, metric emission, structured logging).
Output: Two new modules (otel.py, heartbeat.py), updated requirements.txt/grodt.yml, test files validating both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-python-telemetry-instrumentation/04-CONTEXT.md
@.planning/phases/04-python-telemetry-instrumentation/04-RESEARCH.md
@src/clients/python/requirements.txt
@src/clients/python/engine/trading_engine.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install OTel SDK and create engine/otel.py with setup_otel()</name>
  <files>src/clients/python/requirements.txt, grodt.yml, src/clients/python/engine/otel.py, src/clients/python/tests/test_otel.py</files>
  <read_first>
    - src/clients/python/requirements.txt (current deps)
    - grodt.yml (conda env spec)
    - src/clients/python/engine/__init__.py (existing package)
    - .planning/phases/04-python-telemetry-instrumentation/04-RESEARCH.md (Pattern 1: OTel SDK Setup)
  </read_first>
  <behavior>
    - Test: setup_otel() returns a callable shutdown function
    - Test: After setup_otel(), trace.get_tracer_provider() is a TracerProvider (not NoOpTracerProvider)
    - Test: After setup_otel(), metrics.get_meter_provider() is a MeterProvider (not NoOpMeterProvider)
    - Test: setup_otel() is idempotent -- calling twice does not create duplicate providers (per Pitfall 5)
    - Test: Resource has service.name attribute set to "grodt-strategy" by default
    - Test: shutdown() calls force_flush on both providers without error
  </behavior>
  <action>
    1. Install OTel SDK (per D-08 -- HTTP only, no grpcio):
       ```bash
       /Users/jamal/miniconda3/envs/grodt/bin/pip install opentelemetry-exporter-otlp-proto-http==1.40.0
       ```
    2. Add `opentelemetry-exporter-otlp-proto-http==1.40.0` to requirements.txt (after structlog line)
    3. Add `opentelemetry-exporter-otlp-proto-http==1.40.0` to grodt.yml pip section (if pip section exists, otherwise add to dependencies)
    4. Verify numpy is still 1.26.4:
       ```bash
       /Users/jamal/miniconda3/envs/grodt/bin/python -c "import numpy; print(numpy.__version__)"
       ```
    5. Create src/clients/python/engine/otel.py implementing setup_otel() per D-06, D-10:
       - Function signature: `setup_otel(service_name: str = None, service_version: str = "1.0.0") -> callable`
       - Reads OTEL_EXPORTER_OTLP_ENDPOINT and OTEL_SERVICE_NAME from env vars (D-10)
       - Default service_name: "grodt-strategy" from OTEL_SERVICE_NAME env var
       - Creates Resource with service.name and service.version semconv attributes
       - Creates TracerProvider with BatchSpanProcessor + OTLPSpanExporter (HTTP)
       - Creates MeterProvider with PeriodicExportingMetricReader + OTLPMetricExporter (HTTP)
       - Sets both as global providers via trace.set_tracer_provider() and metrics.set_meter_provider()
       - IDEMPOTENCY GUARD (Pitfall 5): Check if provider is already set via module-level `_initialized` flag. If already initialized, log warning and return existing shutdown callable. Do NOT create duplicate providers.
       - Returns shutdown callable that calls force_flush() then shutdown() on both providers
       - Uses standard `logging` module for any internal log messages (per Pitfall 6)
    6. Create src/clients/python/tests/test_otel.py with unit tests for all behaviors above.
       - Use `opentelemetry.sdk.trace` and `opentelemetry.sdk.metrics` to verify provider types
       - After each test, reset global providers to avoid test pollution:
         ```python
         trace.set_tracer_provider(trace.NoOpTracerProvider())
         metrics.set_meter_provider(metrics.NoOpMeterProvider())
         ```
       - Also reset the `_initialized` flag in otel module between tests
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/test_otel.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "opentelemetry-exporter-otlp-proto-http" src/clients/python/requirements.txt
    - grep -q "setup_otel" src/clients/python/engine/otel.py
    - grep -q "TracerProvider" src/clients/python/engine/otel.py
    - grep -q "MeterProvider" src/clients/python/engine/otel.py
    - grep -q "OTLPSpanExporter" src/clients/python/engine/otel.py
    - grep -q "_initialized" src/clients/python/engine/otel.py
    - grep -q "def test_" src/clients/python/tests/test_otel.py
    - /Users/jamal/miniconda3/envs/grodt/bin/python -c "import numpy; assert numpy.__version__ == '1.26.4'"
    - /Users/jamal/miniconda3/envs/grodt/bin/python -c "from opentelemetry import trace, metrics"
  </acceptance_criteria>
  <done>
    - opentelemetry-exporter-otlp-proto-http==1.40.0 installed in grodt env
    - numpy still at 1.26.4
    - engine/otel.py exists with setup_otel() that creates TracerProvider + MeterProvider
    - setup_otel() is idempotent (safe for optimizer multi-call)
    - All test_otel.py tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create engine/heartbeat.py with StrategyHeartbeat daemon thread</name>
  <files>src/clients/python/engine/heartbeat.py, src/clients/python/tests/test_heartbeat.py</files>
  <read_first>
    - src/clients/python/engine/otel.py (just created in Task 1 -- uses its meter)
    - .planning/phases/04-python-telemetry-instrumentation/04-RESEARCH.md (Pattern 4: Heartbeat Daemon Thread)
    - src/go/telemetry/heartbeat.go (Go heartbeat pattern to mirror)
  </read_first>
  <behavior>
    - Test: StrategyHeartbeat constructor creates gauge metric named "grodt.strategy.heartbeat"
    - Test: After start(), heartbeat thread is alive and is a daemon thread
    - Test: After stop(), heartbeat thread terminates within 2 seconds
    - Test: record_tick() increments tick_count and updates last_tick_time
    - Test: set_state() updates state attribute
    - Test: Heartbeat emits structured log with fields: event, strategy_name, state, tick_count, last_tick_time, uptime_seconds (per D-12)
  </behavior>
  <action>
    Create src/clients/python/engine/heartbeat.py implementing StrategyHeartbeat per D-11, D-12, D-13, D-14:
    - Class: StrategyHeartbeat
    - Constructor args: strategy_name (str)
    - Attributes: strategy_name, state ("idle" default), tick_count (0), last_tick_time (None), _stop_event (threading.Event), _start_time (time.time())
    - Creates gauge metric: `meter.create_gauge("grodt.strategy.heartbeat", unit="1", description="Strategy heartbeat (1=alive)")` via `metrics.get_meter("grodt-strategy")`
    - Creates logger: `logging.getLogger("grodt.strategy.heartbeat")`
    - start() method: creates daemon thread running _run(), stores thread reference
    - _run() method: loops on `_stop_event.wait(timeout=30)`, on each wake:
      - Sets gauge to 1 with attributes {strategy_name, state} (D-13)
      - Emits structured log with extra={event: "heartbeat", strategy_name, state, tick_count, last_tick_time, uptime_seconds} (D-12)
    - stop() method: sets _stop_event, joins thread with timeout=2
    - record_tick() method: increments tick_count, sets last_tick_time to current ISO timestamp
    - set_state(state: str) method: updates self.state
    - Per D-14: NO signal counts or position data in heartbeat

    Create src/clients/python/tests/test_heartbeat.py:
    - Mock the OTel meter to avoid needing real provider
    - Use short timeout (0.1s) instead of 30s for testing by patching the wait timeout
    - Verify thread lifecycle (start, stop, daemon flag)
    - Verify record_tick increments and updates timestamp
    - Verify structured log fields via logging capture
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/test_heartbeat.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class StrategyHeartbeat" src/clients/python/engine/heartbeat.py
    - grep -q "grodt.strategy.heartbeat" src/clients/python/engine/heartbeat.py
    - grep -q "daemon=True" src/clients/python/engine/heartbeat.py
    - grep -q "_stop_event" src/clients/python/engine/heartbeat.py
    - grep -q "strategy_name" src/clients/python/engine/heartbeat.py
    - grep -q "tick_count" src/clients/python/engine/heartbeat.py
    - grep -q "uptime_seconds" src/clients/python/engine/heartbeat.py
    - grep -q "def test_" src/clients/python/tests/test_heartbeat.py
  </acceptance_criteria>
  <done>
    - StrategyHeartbeat class exists with start/stop/record_tick/set_state methods
    - Background daemon thread emits gauge metric "grodt.strategy.heartbeat" every 30s
    - Structured heartbeat log includes strategy_name, state, tick_count, last_tick_time, uptime_seconds
    - All test_heartbeat.py tests pass
  </done>
</task>

</tasks>

<verification>
```bash
# All tests pass
cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/test_otel.py tests/test_heartbeat.py -x -v

# OTel importable
/Users/jamal/miniconda3/envs/grodt/bin/python -c "from opentelemetry import trace, metrics; from engine.otel import setup_otel; from engine.heartbeat import StrategyHeartbeat"

# numpy intact
/Users/jamal/miniconda3/envs/grodt/bin/python -c "import numpy; assert numpy.__version__ == '1.26.4', f'numpy version mismatch: {numpy.__version__}'"
```
</verification>

<success_criteria>
- OTel Python SDK installed and importable without grpcio dependency
- numpy remains at 1.26.4
- engine/otel.py setup_otel() creates TracerProvider + MeterProvider
- engine/heartbeat.py StrategyHeartbeat emits gauge + structured log every 30s
- All new tests pass, existing tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/04-python-telemetry-instrumentation/04-01-SUMMARY.md`
</output>
