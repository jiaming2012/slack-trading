---
phase: 14-core-performance-dashboards
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified: []
autonomous: false
requirements: [DASH-01, DASH-02, DASH-04]
user_setup:
  - service: metabase
    why: "Run SQL migration and provisioning script against live Metabase"
    env_vars:
      - name: METABASE_PASSWORD
        source: "Metabase admin password for jac475@cornell.edu"
    dashboard_config:
      - task: "Ensure Metabase is running at 192.168.8.164:3001"
        location: "Windows Desktop Docker"

must_haves:
  truths:
    - "Operator can select a playground from dropdown and see P&L, win rate, profit factor, equity curve in Trading Performance dashboard"
    - "Operator can view open/close/total slippage per trade in Slippage Analysis dashboard"
    - "Operator can see position history and per-symbol P&L breakdown in Portfolio Analytics dashboard"
    - "Dashboard numbers match v_playground_stats query output for the same playground"
  artifacts: []
  key_links:
    - from: "Metabase dashboards"
      to: "v_playground_stats, v_order_pnl, v_all_slippage, v_trade_fills, equity_plot_records"
      via: "Native SQL questions through metabase_ro user"
      pattern: "playground_id filter"
---

<objective>
Deploy SQL migration and run provisioning script against live Metabase, then verify all 3 dashboards display correct data.

Purpose: Validate that the programmatically-created dashboards work end-to-end with real trading data and match expected metrics.

Output: 3 working Metabase dashboards accessible at 192.168.8.164:3001.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-core-performance-dashboards/14-CONTEXT.md
@.planning/phases/14-core-performance-dashboards/14-01-SUMMARY.md

@infra/analytics-schema.sql
@infra/provision-metabase.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run SQL migration and provisioning script</name>
  <files>infra/analytics-schema.sql, infra/provision-metabase.py</files>
  <read_first>
    - infra/analytics-schema.sql (verify v_close_slippage and v_all_slippage views exist)
    - infra/provision-metabase.py (verify script exists and is valid)
  </read_first>
  <action>
The user must run these commands against the live database and Metabase instance:

**Step 1: Apply SQL migration** (adds close slippage views):
```bash
psql -h 159.89.226.131 -U grodt -d playground --single-transaction=off -f infra/analytics-schema.sql
```
Verify the new views exist:
```sql
SELECT table_name FROM information_schema.views WHERE table_schema = 'public' AND table_name LIKE 'v_%';
```
Expected: v_trade_fills, v_order_pnl, v_playground_stats, v_open_slippage, v_close_slippage, v_all_slippage

**Step 2: Run provisioning script**:
```bash
/Users/jamal/miniconda3/envs/grodt/bin/python infra/provision-metabase.py \
  --url http://192.168.8.164:3001 \
  --user jac475@cornell.edu \
  --password <METABASE_PASSWORD>
```
Script should print created/updated status for each card and dashboard.

If Metabase API returns errors, share the output for diagnosis.
  </action>
  <verify>Script output shows all cards and dashboards created/updated without errors</verify>
  <acceptance_criteria>
    - SQL migration runs without errors
    - v_close_slippage and v_all_slippage views exist in the database
    - Provisioning script exits 0 with success messages for all 3 dashboards
  </acceptance_criteria>
  <done>SQL views deployed, all 3 Metabase dashboards provisioned via API</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dashboards display correct data</name>
  <files>infra/provision-metabase.py</files>
  <read_first>
    - infra/provision-metabase.py (to know which dashboards and cards were created)
  </read_first>
  <action>
Present the user with verification steps for all 3 Metabase dashboards. The user will open http://192.168.8.164:3001 and verify each dashboard visually, then cross-check one playground's numbers against a direct SQL query.
  </action>
  <what-built>3 Metabase dashboards: Trading Performance, Slippage Analysis, Portfolio Analytics -- all with playground filter</what-built>
  <how-to-verify>
1. Open http://192.168.8.164:3001 in browser
2. Navigate to **Trading Performance** dashboard
3. Select a playground from the Playground filter dropdown (should show client_id names)
4. Verify you see: Total P&L (number), Win Rate (%), Profit Factor (number), Total Trades (number)
5. Verify equity curve chart shows a line with drawdown shading
6. Verify P&L over time chart shows cumulative P&L line
7. Navigate to **Slippage Analysis** dashboard
8. Select same playground -- verify slippage summary table shows open/close rows
9. Verify per-trade slippage detail table has data
10. Navigate to **Portfolio Analytics** dashboard
11. Select same playground -- verify per-symbol P&L table shows symbol breakdown
12. Verify position history table shows chronological trades
13. **Validation check**: For one playground, run `SELECT * FROM v_playground_stats WHERE playground_id = '<id>'` in psql and compare total_pnl, win_rate, profit_factor against the dashboard numbers
  </how-to-verify>
  <verify>User confirms all 3 dashboards show correct data matching SQL views</verify>
  <resume-signal>Type "approved" if all 3 dashboards show correct data, or describe specific issues</resume-signal>
  <acceptance_criteria>
    - All 3 dashboards visible in Metabase
    - Playground filter dropdown works and shows human-readable names
    - Trading Performance shows P&L, win rate, profit factor, equity curve, and drawdown
    - Slippage Analysis shows open and close slippage data
    - Portfolio Analytics shows per-symbol P&L and position history
    - Dashboard numbers match direct SQL query within rounding tolerance
  </acceptance_criteria>
  <done>All 3 dashboards verified with real data, numbers match SQL views within rounding tolerance</done>
</task>

</tasks>

<verification>
1. All 3 dashboards accessible at 192.168.8.164:3001
2. Playground filter works on all dashboards (shows client_id, filters data correctly)
3. Trading Performance: P&L number cards, equity curve with drawdown, cumulative P&L line
4. Slippage Analysis: open + close slippage summary, per-trade detail table
5. Portfolio Analytics: per-symbol P&L breakdown, position history, asset class split
6. Numbers match v_playground_stats for at least 1 playground
</verification>

<success_criteria>
- Operator can select any playground and see its trading performance across all 3 dashboards
- Dashboard numbers match playground SQL views within rounding tolerance
- Equity curve shows drawdown overlay (per D-04)
- All dashboards were created programmatically (reproducible via re-running the script)
</success_criteria>

<output>
After completion, create `.planning/phases/14-core-performance-dashboards/14-02-SUMMARY.md`
</output>
