---
phase: 10-signal-candle-filtering-python-heartbeat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/go/backtester-api/models/playground.go
  - src/clients/python/engine/heartbeat.py
  - src/clients/python/engine/client.py
  - src/clients/python/engine/trading_engine.py
  - observability/dashboards/grodt-live-simulation.json
autonomous: true
requirements: [LABEL-03, LABEL-04, PANEL-02]

must_haves:
  truths:
    - "Signals Generated panel can be filtered by signal_type (e.g., show only mean_reversion)"
    - "Candles Processed panel can be filtered by candle symbol (e.g., show only AAPL)"
    - "Python Strategy Heartbeat panel shows per-playground rows with client_id, state, and uptime"
  artifacts:
    - path: "src/go/backtester-api/models/playground.go"
      provides: "symbol attribute on CandlesProcessed metric"
      contains: "attribute.String(\"symbol\""
    - path: "src/clients/python/engine/heartbeat.py"
      provides: "Per-playground heartbeat with playground_id and client_id attributes"
      contains: "playground_id"
    - path: "src/clients/python/engine/client.py"
      provides: "client_id stored on playground client instance"
      contains: "self.client_id"
    - path: "observability/dashboards/grodt-live-simulation.json"
      provides: "signal_type and symbol template variables, updated panel queries"
      contains: "signal_type"
  key_links:
    - from: "src/go/backtester-api/models/playground.go"
      to: "observability/dashboards/grodt-live-simulation.json"
      via: "CandlesProcessed metric now emits symbol label -> dashboard filters by it"
      pattern: "symbol"
    - from: "src/clients/python/engine/heartbeat.py"
      to: "observability/dashboards/grodt-live-simulation.json"
      via: "heartbeat gauge emits playground_id+client_id -> dashboard table shows per-playground rows"
      pattern: "playground_id"
---

<objective>
Add signal_type and symbol filtering to dashboard panels, and upgrade Python heartbeat to per-playground status.

Purpose: Operator can drill into specific signal types, filter candle processing by symbol, and see which playgrounds have active heartbeats (not just a binary alive/dead indicator).

Output: Updated Go metric emission, Python heartbeat with playground dimensions, and Grafana dashboard JSON with new template variables and updated panels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-metric-labels-bug-fix/08-01-SUMMARY.md
@.planning/phases/09-playground-filtering-detail-panels/09-01-SUMMARY.md

@src/go/telemetry/metrics.go
@src/go/backtester-api/models/playground.go
@src/go/backtester-api/models/backtester_candle.go
@src/go/backtester-api/router/grpc.go
@src/clients/python/engine/heartbeat.py
@src/clients/python/engine/client.py
@src/clients/python/engine/trading_engine.py
@observability/dashboards/grodt-live-simulation.json

<interfaces>
From src/go/telemetry/metrics.go:
```go
func PlaygroundAttrs(env string, accountType string, clientID string) metric.MeasurementOption
func ClientIDOrEmpty(clientID *string) string
var CandlesProcessed metric.Int64Counter
var SignalsGenerated metric.Int64Counter
```

From src/go/backtester-api/models/backtester_candle.go:
```go
type BacktesterCandle struct {
    Symbol eventmodels.Instrument `json:"symbol"`
    Period time.Duration          `json:"period"`
    Bar    *eventmodels.AggregateBarWithIndicators `json:"candle"`
}
```

From src/go/eventmodels/instrument.go:
```go
type Instrument interface {
    GetTicker() string
}
```

From src/clients/python/engine/heartbeat.py:
```python
class StrategyHeartbeat:
    def __init__(self, strategy_name: str):
    def start(self):
    def stop(self):
    def record_tick(self):
    def set_state(self, state: str):
```

From src/clients/python/engine/client.py:
```python
class BacktesterPlaygroundClient:
    # self.id = playground UUID (set in __init__)
    # req.client_id = optional client ID from CreatePolygonPlaygroundRequest
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add symbol attribute to CandlesProcessed metric and store client_id on Python playground client</name>
  <files>src/go/backtester-api/models/playground.go, src/clients/python/engine/client.py</files>
  <action>
**Go — Add symbol to CandlesProcessed metric (LABEL-04):**

In `src/go/backtester-api/models/playground.go`, find the CandlesProcessed emission in the `liveTick` method (around line 2314). Currently it uses only `PlaygroundAttrs`. Add the symbol from the dequeued candle:

Change:
```go
if telemetry.CandlesProcessed != nil {
    telemetry.CandlesProcessed.Add(context.Background(), 1,
        telemetry.PlaygroundAttrs(string(p.Meta.Environment), string(p.Meta.LiveAccountType), telemetry.ClientIDOrEmpty(p.GetClientId())))
}
```

To:
```go
if telemetry.CandlesProcessed != nil {
    telemetry.CandlesProcessed.Add(context.Background(), 1,
        telemetry.PlaygroundAttrs(string(p.Meta.Environment), string(p.Meta.LiveAccountType), telemetry.ClientIDOrEmpty(p.GetClientId())),
        metric.WithAttributes(
            attribute.String("symbol", candle.Symbol.GetTicker()),
        ))
}
```

Ensure `metric` and `attribute` imports are present (they should be from the existing `go.opentelemetry.io/otel/metric` and `go.opentelemetry.io/otel/attribute` packages). The `candle` variable is the loop variable from `p.GetNewCandlesQueue().Dequeue()`.

**Python — Store client_id on BacktesterPlaygroundClient (PANEL-02 support):**

In `src/clients/python/engine/client.py`, in the `__init__` method of `BacktesterPlaygroundClient`, after `self.environment = req.environment` (around line 316), add:
```python
self.client_id = getattr(req, 'client_id', '') or ''
```

This makes client_id accessible from the playground object for the heartbeat in Task 2.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading && go build ./src/go/backtester-api/models/</automated>
  </verify>
  <done>CandlesProcessed metric includes symbol attribute from candle.Symbol.GetTicker(). BacktesterPlaygroundClient stores self.client_id.</done>
</task>

<task type="auto">
  <name>Task 2: Update Python heartbeat to emit per-playground metrics and update run_strategy</name>
  <files>src/clients/python/engine/heartbeat.py, src/clients/python/engine/trading_engine.py</files>
  <action>
**Update StrategyHeartbeat for per-playground identification (PANEL-02):**

In `src/clients/python/engine/heartbeat.py`:

1. Change `__init__` signature to accept playground dimensions:
```python
def __init__(self, strategy_name: str, playground_id: str = "", client_id: str = ""):
    self.strategy_name = strategy_name
    self.playground_id = playground_id
    self.client_id = client_id
    # ... rest unchanged
```

2. Update `_emit_heartbeat` to include playground_id and client_id in gauge attributes:
```python
self._heartbeat_gauge.set(
    1,
    attributes={
        "strategy_name": self.strategy_name,
        "state": self.state,
        "playground_id": self.playground_id,
        "client_id": self.client_id,
    },
)
```

3. Update the structured log to include playground_id and client_id:
```python
logger.info(
    "heartbeat | strategy={} state={} ticks={} uptime={}s playground_id={} client_id={}",
    self.strategy_name, self.state, self.tick_count, uptime_seconds,
    self.playground_id, self.client_id,
)
```

**Update run_strategy to pass playground dimensions:**

In `src/clients/python/engine/trading_engine.py`, update the heartbeat construction (around line 103):

Change:
```python
heartbeat = StrategyHeartbeat(strategy_name)
```

To:
```python
heartbeat = StrategyHeartbeat(
    strategy_name,
    playground_id=getattr(playground, 'id', ''),
    client_id=getattr(playground, 'client_id', ''),
)
```
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading && /Users/jamal/miniconda3/envs/grodt/bin/python -c "from engine.heartbeat import StrategyHeartbeat; hb = StrategyHeartbeat('test', playground_id='abc', client_id='xyz'); print('OK')"</automated>
  </verify>
  <done>StrategyHeartbeat accepts and emits playground_id and client_id. run_strategy passes playground dimensions to heartbeat.</done>
</task>

<task type="auto">
  <name>Task 3: Add signal_type and symbol template variables and update dashboard panels</name>
  <files>observability/dashboards/grodt-live-simulation.json</files>
  <action>
**Add two new template variables to the dashboard (LABEL-03, LABEL-04):**

In the `templating.list` array, after the existing `playground_id` variable, add two new variables:

1. **signal_type variable** — sourced from `grodt_signals_generated_total`:
```json
{
    "allValue": ".*",
    "current": {"selected": true, "text": "All", "value": "$__all"},
    "datasource": {"type": "prometheus", "uid": "prometheus"},
    "definition": "label_values(grodt_signals_generated_total, signal_type)",
    "includeAll": true,
    "label": "Signal Type",
    "multi": false,
    "name": "signal_type",
    "query": {"qryType": 1, "query": "label_values(grodt_signals_generated_total, signal_type)"},
    "refresh": 2,
    "sort": 1,
    "type": "query"
}
```

2. **symbol variable** — sourced from `grodt_candles_processed_total`:
```json
{
    "allValue": ".*",
    "current": {"selected": true, "text": "All", "value": "$__all"},
    "datasource": {"type": "prometheus", "uid": "prometheus"},
    "definition": "label_values(grodt_candles_processed_total, symbol)",
    "includeAll": true,
    "label": "Symbol",
    "multi": false,
    "name": "symbol",
    "query": {"qryType": 1, "query": "label_values(grodt_candles_processed_total, symbol)"},
    "refresh": 2,
    "sort": 1,
    "type": "query"
}
```

**Update Signals Generated panel query (LABEL-03):**

Find the panel with `"title": "Signals Generated"` (id 11). Change its target expr from:
```
sum(grodt_signals_generated_total) or sum(grodt_signals_generated_total_ratio)
```
To:
```
sum by (signal_type) (grodt_signals_generated_total{signal_type=~"$signal_type", client_id=~"$client_id"})
```
Update legendFormat to `{{signal_type}}` so each signal type gets its own line.

**Update Candles Processed panel query (LABEL-04):**

Find the panel with `"title": "Candles Processed"` (id 10). Change its target expr from:
```
sum(grodt_candles_processed_total)
```
To:
```
sum by (symbol) (grodt_candles_processed_total{symbol=~"$symbol", client_id=~"$client_id"})
```
Update legendFormat to `{{symbol}}` so each symbol gets its own line.

**Update Python Strategy Heartbeat panel to table (PANEL-02):**

Find the panel with `"title": "Python Strategy Heartbeat"` (id 3). Replace the entire panel definition:

- Change `"type"` from `"stat"` to `"table"`
- Replace fieldConfig: remove the value mappings (Alive/Dead) and thresholds. Use:
```json
"fieldConfig": {
    "defaults": {"custom": {"filterable": true}},
    "overrides": [
        {"matcher": {"id": "byName", "options": "Time"}, "properties": [{"id": "custom.hidden", "value": true}]},
        {"matcher": {"id": "byName", "options": "__name__"}, "properties": [{"id": "custom.hidden", "value": true}]},
        {"matcher": {"id": "byName", "options": "Value"}, "properties": [{"id": "custom.hidden", "value": true}]},
        {"matcher": {"id": "byName", "options": "job"}, "properties": [{"id": "custom.hidden", "value": true}]},
        {"matcher": {"id": "byName", "options": "instance"}, "properties": [{"id": "custom.hidden", "value": true}]}
    ]
}
```
- Replace options with: `{"showHeader": true, "sortBy": [{"displayName": "client_id", "desc": false}]}`
- Change the target expr to: `grodt_strategy_heartbeat{client_id=~"$client_id"}` (no sum -- we want per-playground rows)
- Set `"format": "table"` and `"instant": true` on the target
- Remove the legendFormat or set to empty string
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading && /Users/jamal/miniconda3/envs/grodt/bin/python -c "import json; d=json.load(open('observability/dashboards/grodt-live-simulation.json')); vs=[v['name'] for v in d['templating']['list']]; assert 'signal_type' in vs, f'missing signal_type in {vs}'; assert 'symbol' in vs, f'missing symbol in {vs}'; panels={p['title']:p for p in d['panels'] if 'title' in p}; hb=panels['Python Strategy Heartbeat']; assert hb['type']=='table', f'expected table got {hb[\"type\"]}'; print('OK: signal_type and symbol vars present, heartbeat is table')"</automated>
  </verify>
  <done>Dashboard has signal_type and symbol template variables. Signals Generated filters by signal_type with per-type legend. Candles Processed filters by symbol with per-symbol legend. Python Strategy Heartbeat is a table showing per-playground rows with client_id, strategy_name, state columns.</done>
</task>

</tasks>

<verification>
1. `go build ./src/go/backtester-api/models/` compiles without errors
2. Dashboard JSON is valid and contains all 4 template variables (client_id, playground_id, signal_type, symbol)
3. Python heartbeat constructor accepts playground_id and client_id
4. Heartbeat panel type is "table" (not "stat")
5. Signals Generated query includes `signal_type=~"$signal_type"` filter
6. Candles Processed query includes `symbol=~"$symbol"` filter
</verification>

<success_criteria>
- LABEL-03: Signals Generated panel has signal_type dropdown filter and shows per-type breakdown
- LABEL-04: Candles Processed panel has symbol dropdown filter and shows per-symbol breakdown
- PANEL-02: Python Strategy Heartbeat panel is a table showing per-playground status with client_id, strategy_name, state columns
</success_criteria>

<output>
After completion, create `.planning/phases/10-signal-candle-filtering-python-heartbeat/10-01-SUMMARY.md`
</output>
