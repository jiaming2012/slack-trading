---
phase: 17-signal-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/go/eventmodels/trade_signal.go
  - src/go/eventmodels/signal_name.go
  - src/go/eventmodels/stream_names.go
  - src/go/eventmodels/event_names.go
  - src/go/playground.proto
  - src/go/playground/playground.pb.go
  - src/go/playground/playground.twirp.go
  - src/clients/python/rpc/playground_pb2.py
  - src/clients/python/rpc/playground_twirp.py
  - src/go/eventmodels/trade_signal_test.go
autonomous: true
requirements:
  - SIG-01
  - SIG-03

must_haves:
  truths:
    - "TradeSignal struct exists with ID, Name, Symbol, Timestamp, and Attributes fields"
    - "SignalName is a typed string constant registry with Validate() method"
    - "TradeSignalProto message exists in proto with map<string, string> attributes"
    - "TradeSignal implements SavedEvent interface for future ESDB persistence"
    - "Proto stubs are regenerated for Go and Python"
  artifacts:
    - path: "src/go/eventmodels/trade_signal.go"
      provides: "TradeSignal struct with SavedEvent implementation"
      exports: ["TradeSignal", "NewTradeSignal"]
    - path: "src/go/eventmodels/signal_name.go"
      provides: "SignalName type and constant registry"
      exports: ["SignalName", "SignalMACrossover", "SignalStartOfWeek", "SignalCoveredCall", "SignalMeanReversion"]
    - path: "src/go/playground.proto"
      provides: "TradeSignalProto message definition"
      contains: "message TradeSignalProto"
    - path: "src/go/eventmodels/trade_signal_test.go"
      provides: "Unit tests for TradeSignal and SignalName"
  key_links:
    - from: "src/go/eventmodels/trade_signal.go"
      to: "src/go/eventmodels/signal_name.go"
      via: "Name field uses SignalName type"
      pattern: "Name\\s+SignalName"
    - from: "src/go/eventmodels/trade_signal.go"
      to: "src/go/eventmodels/stream_names.go"
      via: "GetSavedEventParameters references TradeSignalStream"
      pattern: "TradeSignalStream"
---

<objective>
Define the canonical TradeSignal type in Go and proto, establishing the signal name registry and ESDB-ready struct that all downstream phases build upon.

Purpose: SIG-01 and SIG-03 require the TradeSignal struct to exist in Go models and proto with flexible attributes. This plan creates the foundation types without touching any existing order flow.
Output: New Go files (trade_signal.go, signal_name.go), proto TradeSignalProto message, regenerated stubs, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-signal-foundation/17-CONTEXT.md
@.planning/phases/17-signal-foundation/17-RESEARCH.md

@src/go/eventmodels/stream_names.go
@src/go/eventmodels/event_names.go
@src/go/eventmodels/saved_event.go
@src/go/eventmodels/saved_event_parameters.go
@src/go/eventmodels/base_request_event.go
@src/go/eventmodels/tracker_v3.go
@src/go/playground.proto

<interfaces>
<!-- Existing types the executor needs to implement against -->

From src/go/eventmodels/base_request_event.go:
```go
type BaseRequestEvent struct {
    Meta MetaData `json:"meta"`
}
```

From src/go/eventmodels/saved_event.go:
```go
type SavedEvent interface {
    GetSavedEventParameters() SavedEventParameters
    GetMetaData() *MetaData
}
```

From src/go/eventmodels/saved_event_parameters.go:
```go
type SavedEventParameters struct {
    StreamName    StreamName
    EventName     EventName
    SchemaVersion int
}
```

From src/go/eventmodels/stream_names.go:
```go
type StreamName string
// Existing constants: AccountsStream, OptionAlertsStream, etc.
// Existing per-symbol pattern: func NewCandleStreamName(symbol string) StreamName
```

From src/go/eventmodels/event_names.go:
```go
// type EventName string (defined in event_name.go)
// Constants: NewTickEventName, CreateTrackerEvent, etc.
```

From src/go/eventmodels/tracker_v3.go (SavedEvent pattern to follow):
```go
type TrackerV3 struct {
    BaseRequestEvent
    Type       TrackerType `json:"type"`
    streamName StreamName  `json:"-"`
}

func (c *TrackerV3) GetSavedEventParameters() SavedEventParameters {
    return SavedEventParameters{
        StreamName:    c.streamName,
        EventName:     CreateTrackerEvent,
        SchemaVersion: 3,
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SignalName registry and TradeSignal struct</name>
  <files>
    src/go/eventmodels/signal_name.go,
    src/go/eventmodels/trade_signal.go,
    src/go/eventmodels/stream_names.go,
    src/go/eventmodels/event_names.go,
    src/go/eventmodels/trade_signal_test.go
  </files>
  <read_first>
    src/go/eventmodels/stream_names.go,
    src/go/eventmodels/event_names.go,
    src/go/eventmodels/saved_event.go,
    src/go/eventmodels/saved_event_parameters.go,
    src/go/eventmodels/base_request_event.go,
    src/go/eventmodels/tracker_v3.go,
    src/go/eventmodels/event_name.go
  </read_first>
  <behavior>
    - Test 1: NewTradeSignal creates a signal with non-zero UUID, correct Name, Symbol, Timestamp, and Attributes
    - Test 2: TradeSignal.GetSavedEventParameters returns StreamName "trade-signals-AAPL" for symbol AAPL, EventName "TradeSignalEvent", SchemaVersion 1
    - Test 3: SignalName.Validate returns nil for known signals (SignalMACrossover, SignalStartOfWeek, SignalCoveredCall, SignalMeanReversion)
    - Test 4: SignalName.Validate returns error for unknown signal name "invalid_signal"
    - Test 5: TradeSignal JSON round-trip preserves all fields including map[string]interface{} attributes with string, float64, and nested values
  </behavior>
  <action>
    Create `src/go/eventmodels/signal_name.go` (per D-01):
    - `package eventmodels`
    - `type SignalName string`
    - const block with: `SignalMACrossover SignalName = "ma_crossover"`, `SignalStartOfWeek SignalName = "start_of_week"`, `SignalCoveredCall SignalName = "covered_call"`, `SignalMeanReversion SignalName = "mean_reversion"`
    - `func (s SignalName) Validate() error` -- switch on known constants, return `fmt.Errorf("unknown signal name: %s", s)` for default

    Create `src/go/eventmodels/trade_signal.go` (per SIG-01, SIG-03, D-02):
    - `package eventmodels`
    - imports: `"time"`, `"fmt"`, `"github.com/google/uuid"`
    - `type TradeSignal struct` embedding `BaseRequestEvent`, with fields:
      - `ID uuid.UUID` json:"id"
      - `Name SignalName` json:"name"
      - `Symbol StockSymbol` json:"symbol"
      - `Timestamp time.Time` json:"timestamp"
      - `Attributes map[string]interface{}` json:"attributes" (per SIG-03 and D-02)
      - `streamName StreamName` json:"-"
    - `func NewTradeSignal(name SignalName, symbol StockSymbol, timestamp time.Time, attributes map[string]interface{}) *TradeSignal` -- generates uuid.New() for ID, sets streamName via NewTradeSignalStreamName
    - `func (s *TradeSignal) GetSavedEventParameters() SavedEventParameters` -- returns SavedEventParameters with StreamName from NewTradeSignalStreamName(string(s.Symbol)), EventName TradeSignalEventName, SchemaVersion 1

    Append to `src/go/eventmodels/stream_names.go`:
    - Add constant: `TradeSignalStream StreamName = "trade-signals"`
    - Add function: `func NewTradeSignalStreamName(symbol string) StreamName` returning `StreamName(fmt.Sprintf("%s-%s", TradeSignalStream, symbol))`

    Append to `src/go/eventmodels/event_names.go`:
    - Add constant: `TradeSignalEventName EventName = "TradeSignalEvent"`

    Create `src/go/eventmodels/trade_signal_test.go`:
    - `package eventmodels_test`
    - imports: `"testing"`, `"time"`, `"encoding/json"`, `"github.com/stretchr/testify/assert"`, `"github.com/stretchr/testify/require"`, `"github.com/google/uuid"`, eventmodels package
    - Implement tests for all 5 behaviors listed above
    - For JSON round-trip test, marshal then unmarshal TradeSignal and assert all fields match (note: json unmarshal will decode numbers as float64 in map[string]interface{})
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading && go test ./src/go/eventmodels/ -run "TestTradeSignal|TestSignalName" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - File src/go/eventmodels/signal_name.go exists with `type SignalName string` and 4 constants
    - File src/go/eventmodels/trade_signal.go exists with TradeSignal struct embedding BaseRequestEvent
    - TradeSignal.Attributes is `map[string]interface{}` (not map[string]string)
    - stream_names.go contains `TradeSignalStream StreamName = "trade-signals"` and NewTradeSignalStreamName function
    - event_names.go contains `TradeSignalEventName EventName = "TradeSignalEvent"`
    - trade_signal_test.go exists with all 5 tests passing
    - `go build ./src/go/eventmodels/` succeeds
  </acceptance_criteria>
  <done>TradeSignal struct and SignalName registry exist with SavedEvent implementation. All unit tests pass. Attributes use map[string]interface{} per SIG-03.</done>
</task>

<task type="auto">
  <name>Task 2: Add TradeSignalProto to proto and regenerate stubs</name>
  <files>
    src/go/playground.proto,
    src/go/playground/playground.pb.go,
    src/go/playground/playground.twirp.go,
    src/clients/python/rpc/playground_pb2.py,
    src/clients/python/rpc/playground_twirp.py
  </files>
  <read_first>
    src/go/playground.proto
  </read_first>
  <action>
    Edit `src/go/playground.proto`:

    1. Add new TradeSignalProto message (place it near other message definitions, before the Order message):
    ```protobuf
    message TradeSignalProto {
        string id = 1;
        string name = 2;
        string symbol = 3;
        string timestamp = 4;
        map<string, string> attributes = 5;
    }
    ```
    Note: Proto uses `map<string, string>` per D-02 (wire format). The Go internal struct uses `map[string]interface{}`. Conversion happens at the RPC boundary in Phase 19.

    2. Add `optional string signal_id = 17;` to PlaceOrderRequest (after `string trace_id = 16;` on the last line). Field 17 is verified unused.

    3. Add `optional string signal_id = 26;` to Order message (after `optional double pl = 25;` on the last line). Field 26 is verified unused.

    4. Add deprecation comment to RecordSignalRequest message (per D-04):
    ```protobuf
    // Deprecated: Use WriteSignal RPC (Phase 19) instead. Kept functional until Phase 22 migration complete.
    message RecordSignalRequest {
    ```

    5. Run `task gen:proto` to regenerate Go and Python stubs.

    6. Verify generated files exist and contain new fields:
    - `src/go/playground/playground.pb.go` should contain `TradeSignalProto` struct and `SignalId` field on `PlaceOrderRequest` and `Order`
    - `src/clients/python/rpc/playground_pb2.py` should be updated

    7. Run `go build ./src/go/...` to confirm no compilation errors after regeneration.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading && task gen:proto && go build ./src/go/...</automated>
  </verify>
  <acceptance_criteria>
    - playground.proto contains `message TradeSignalProto` with 5 fields (id, name, symbol, timestamp, attributes)
    - playground.proto PlaceOrderRequest has `optional string signal_id = 17`
    - playground.proto Order message has `optional string signal_id = 26`
    - RecordSignalRequest has deprecation comment mentioning WriteSignal and Phase 19
    - `task gen:proto` succeeds without errors
    - `go build ./src/go/...` succeeds
    - Generated playground.pb.go contains `TradeSignalProto` type
  </acceptance_criteria>
  <done>TradeSignalProto proto message defined. signal_id fields on PlaceOrderRequest (field 17) and Order (field 26). RecordSignal marked deprecated. All stubs regenerated and compilation passes.</done>
</task>

</tasks>

<verification>
- `go build ./src/go/...` compiles all packages
- `go test ./src/go/eventmodels/ -run "TestTradeSignal|TestSignalName" -v -count=1` passes
- `grep -r "TradeSignalProto" src/go/playground/playground.pb.go` finds the generated struct
- `grep "signal_id" src/go/playground.proto` shows field 17 on PlaceOrderRequest and field 26 on Order
</verification>

<success_criteria>
- TradeSignal Go struct exists in eventmodels with Name (SignalName), Attributes (map[string]interface{}), ID (uuid.UUID), Symbol, Timestamp
- SignalName typed string registry has 4 constants and Validate() method
- TradeSignalProto proto message exists with map<string, string> attributes
- signal_id fields exist on PlaceOrderRequest and Order proto messages
- All Go packages compile, all new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/17-signal-foundation/17-01-SUMMARY.md`
</output>
