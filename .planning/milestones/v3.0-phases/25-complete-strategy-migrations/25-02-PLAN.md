---
phase: 25-complete-strategy-migrations
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - src/clients/python/datasources/pdf_wheel_signals.py
  - src/clients/python/strategies/pdf_wheel_v2.py
  - src/clients/python/tests/test_pdf_wheel_diff.py
autonomous: true
requirements: [MIG-01]

must_haves:
  truths:
    - "PDFWheelStrategyV2 delegates signal detection to pdf_wheel_signals datasource"
    - "PDFWheelV2 extends WheelStrategyV2 (not WheelV1)"
    - "Diff test proves V1/V2 equivalence for PDFWheel"
  artifacts:
    - path: "src/clients/python/datasources/pdf_wheel_signals.py"
      provides: "PDF-guided signal extraction for wheel (HTF + LTF compound signals)"
      contains: "def produce_signals"
    - path: "src/clients/python/strategies/pdf_wheel_v2.py"
      provides: "V2 PDF wheel strategy extending WheelStrategyV2"
      contains: "class PDFWheelStrategyV2"
    - path: "src/clients/python/tests/test_pdf_wheel_diff.py"
      provides: "Behavioral diff test: V1 vs V2 PDFWheel"
      contains: "def test_"
  key_links:
    - from: "src/clients/python/strategies/pdf_wheel_v2.py"
      to: "src/clients/python/strategies/wheel_v2.py"
      via: "extends WheelStrategyV2"
      pattern: "class PDFWheelStrategyV2\\(WheelStrategyV2\\)"
    - from: "src/clients/python/strategies/pdf_wheel_v2.py"
      to: "src/clients/python/datasources/pdf_wheel_signals.py"
      via: "import produce_signals"
      pattern: "from datasources.pdf_wheel_signals import"
---

<objective>
Migrate PDFWheelStrategy to V2. PDFWheel extends WheelStrategy and adds PDF-based compound signal detection (detect_atomic_signals_on_bar). The V2 must extend WheelStrategyV2 from Plan 01 per D-06.

Purpose: PDFWheel is the last strategy with an inheritance dependency. Completing this means ALL strategies have V2 variants.
Output: pdf_wheel_signals datasource, PDFWheelStrategyV2, diff test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-complete-strategy-migrations/25-CONTEXT.md
@.planning/phases/25-complete-strategy-migrations/25-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (will exist when this plan runs) -->

From src/clients/python/strategies/wheel_v2.py (created in Plan 01):
```python
class WheelStrategyV2(OptionsStrategyBasicV2):
    # Phase 1: put sell via wheel_signals datasource
    # Phase 2: covered call via parent OptionsStrategyBasicV2
```

From src/clients/python/strategies/covered_call_v2.py (created in Plan 01):
```python
class OptionsStrategyBasicV2(BaseStrategy):
    label = "covered_call"
```

<!-- PDFWheel V1 signal detection (to extract) -->

From src/clients/python/strategies/pdf_wheel.py:
```python
class PDFWheelStrategy(WheelStrategy):
    # Uses detect_atomic_signals_on_bar for HTF compound signals
    # Uses PDF lookup for signal validation
    # Sells puts at multiple strike levels based on PDF percentiles
```

From src/clients/python/datasources/credit_spread_signals.py (similar pattern):
```python
def produce_signals(bar_dict, prev_bar, pdf, htf_horizon="1h") -> list:
    signals = detect_atomic_signals_on_bar(bar_dict, prev_bar, timeframe="ltf")
    # ... PDF lookup, filtering ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PDFWheel datasource + V2 strategy + diff test</name>
  <files>
    src/clients/python/datasources/pdf_wheel_signals.py
    src/clients/python/strategies/pdf_wheel_v2.py
    src/clients/python/tests/test_pdf_wheel_diff.py
  </files>
  <read_first>
    src/clients/python/strategies/pdf_wheel.py
    src/clients/python/strategies/wheel_v2.py
    src/clients/python/strategies/covered_call_v2.py
    src/clients/python/datasources/credit_spread_signals.py
    src/clients/python/tests/test_credit_spread_diff.py
    src/clients/python/datasources/__init__.py
    src/clients/python/lib/pdf_builder.py
    src/clients/python/lib/pdf_types.py
  </read_first>
  <action>
    1. Create `src/clients/python/datasources/pdf_wheel_signals.py`:
       - Extract PDFWheelStrategy's compound signal detection logic
       - PDFWheel uses `detect_atomic_signals_on_bar()` from `lib.pdf_builder` for HTF compound signals
       - Then does PDF lookup to validate signal and get horizon stats
       - Function: `produce_signals(bar_dict, prev_bar, pdf, htf_horizon="1h")` — same pattern as credit_spread_signals.py
       - Returns list of signal dicts with keys: signal_key, bar_dict, pdf_entry, direction, horizon, compound_signals

    2. Create `src/clients/python/strategies/pdf_wheel_v2.py`:
       - Class `PDFWheelStrategyV2(WheelStrategyV2)` per D-06 — extends WheelStrategyV2, NOT WheelStrategy V1
       - Import `from strategies.wheel_v2 import WheelStrategyV2`
       - Import `from datasources.pdf_wheel_signals import produce_signals`
       - Import shared types from V1: `from strategies.pdf_wheel import ...` (any PDF-specific dataclasses)
       - Full copy of V1 PDFWheel logic, replacing inline signal detection with datasource call
       - The PDF-guided put selection at multiple strikes, Kelly sizing, etc. stays in the strategy
       - Only the signal DETECTION (detect_atomic_signals_on_bar + PDF lookup) moves to datasource

    3. Create `src/clients/python/tests/test_pdf_wheel_diff.py`:
       - Follow test_credit_spread_diff.py pattern
       - Import both V1 (PDFWheelStrategy) and V2 (PDFWheelStrategyV2)
       - Patch detect_atomic_signals_on_bar in both source modules
       - Create mock PDFDocument with test signal entries
       - Compare at the signal detection boundary
       - Assert identical signal detection results given identical inputs

    4. Update `src/clients/python/datasources/__init__.py` if needed.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/test_pdf_wheel_diff.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pdf_wheel_signals.py` exists and contains `def produce_signals`
    - `pdf_wheel_v2.py` exists and contains `class PDFWheelStrategyV2(WheelStrategyV2)`
    - `pdf_wheel_v2.py` contains `from datasources.pdf_wheel_signals import`
    - `pdf_wheel_v2.py` contains `from strategies.wheel_v2 import WheelStrategyV2`
    - `test_pdf_wheel_diff.py` has at least 2 test functions and all pass
  </acceptance_criteria>
  <done>PDFWheelStrategyV2 extends WheelStrategyV2, delegates compound signal detection to pdf_wheel_signals datasource; diff test proves V1/V2 equivalence</done>
</task>

</tasks>

<verification>
All 3 new files exist. Diff test passes. Full V2 inheritance chain: PDFWheelStrategyV2 -> WheelStrategyV2 -> OptionsStrategyBasicV2 -> BaseStrategy.
</verification>

<success_criteria>
- pdf_wheel_v2.py extends WheelStrategyV2 (NOT WheelStrategy V1)
- PDFWheel datasource extracts compound signal detection from V1
- Diff test passes proving zero behavioral drift
- Complete V2 inheritance chain is established
</success_criteria>

<output>
After completion, create `.planning/phases/25-complete-strategy-migrations/25-02-SUMMARY.md`
</output>
