---
phase: 25-complete-strategy-migrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/clients/python/datasources/covered_call_signals.py
  - src/clients/python/strategies/covered_call_v2.py
  - src/clients/python/tests/test_covered_call_diff.py
  - src/clients/python/datasources/wheel_signals.py
  - src/clients/python/strategies/wheel_v2.py
  - src/clients/python/tests/test_wheel_diff.py
autonomous: true
requirements: [MIG-01]

must_haves:
  truths:
    - "OptionsStrategyBasicV2 delegates signal detection to covered_call_signals datasource"
    - "WheelStrategyV2 extends OptionsStrategyBasicV2 and delegates put signal detection to wheel_signals datasource"
    - "Diff tests prove V1/V2 equivalence for both strategies"
  artifacts:
    - path: "src/clients/python/datasources/covered_call_signals.py"
      provides: "Supertrend-based signal extraction for covered call"
      contains: "def produce_open_signals"
    - path: "src/clients/python/strategies/covered_call_v2.py"
      provides: "V2 covered call strategy consuming datasource signals"
      contains: "class OptionsStrategyBasicV2"
    - path: "src/clients/python/tests/test_covered_call_diff.py"
      provides: "Behavioral diff test: V1 vs V2 covered call"
      contains: "def test_"
    - path: "src/clients/python/datasources/wheel_signals.py"
      provides: "Put sell signal extraction for wheel strategy"
      contains: "def produce_open_signals"
    - path: "src/clients/python/strategies/wheel_v2.py"
      provides: "V2 wheel strategy extending OptionsStrategyBasicV2"
      contains: "class WheelStrategyV2"
    - path: "src/clients/python/tests/test_wheel_diff.py"
      provides: "Behavioral diff test: V1 vs V2 wheel"
      contains: "def test_"
  key_links:
    - from: "src/clients/python/strategies/covered_call_v2.py"
      to: "src/clients/python/datasources/covered_call_signals.py"
      via: "import produce_open_signals"
      pattern: "from datasources.covered_call_signals import"
    - from: "src/clients/python/strategies/wheel_v2.py"
      to: "src/clients/python/strategies/covered_call_v2.py"
      via: "extends OptionsStrategyBasicV2"
      pattern: "class WheelStrategyV2\\(OptionsStrategyBasicV2\\)"
---

<objective>
Rebuild CoveredCall (OptionsStrategyBasic) V2 and WheelStrategy V2 from scratch per D-07. Extract signal detection into datasource modules, create V2 strategy classes that delegate to datasources, and write behavioral diff tests proving V1/V2 equivalence.

Purpose: These files were claimed built in Phase 22-03 but are absent from the branch. Rebuilding from scratch using the proven Phase 21/22 datasource pattern.
Output: 2 datasource modules, 2 V2 strategies, 2 diff test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-complete-strategy-migrations/25-CONTEXT.md
@.planning/phases/22-remaining-strategy-migrations/22-03-SUMMARY.md

<interfaces>
<!-- Existing V2 pattern to follow -->

From src/clients/python/datasources/credit_spread_signals.py:
```python
def produce_signals(bar_dict, prev_bar, pdf, htf_horizon="1h") -> list:
    """Returns list of signal dicts."""
```

From src/clients/python/strategies/credit_spread_v2.py:
```python
class CreditSpreadStrategyV2(BaseStrategy):
    # delegates to: from datasources.credit_spread_signals import produce_signals
    def _process_htf_candle(self, bar_dict: dict) -> None:
        signals = produce_signals(bar_dict, self._prev_bar, self.pdf, self._htf_horizon)
```

From src/clients/python/strategies/base_strategy.py:
```python
class BaseStrategy(ABC):
    def on_signal(self, signal) -> None:
        """Handle a TradeSignal from TickDelta.new_signals. Default: no-op."""
        pass
```

<!-- CoveredCall signal detection (to extract) -->

From src/clients/python/strategies/covered_call.py:
```python
class OptionsStrategyBasic(BaseOpenStrategyV2, BaseStrategy):
    label = "covered_call"
    def check_for_new_signal(self, new_candle: pd.DataFrame):
        # Uses supertrend, stochrsi indicators on candles_ltf DataFrame
        # Returns OpenSignalV4 or None
```

From src/clients/python/strategies/wheel.py:
```python
class WheelStrategy(OptionsStrategyBasic):
    # Phase 1 (SELL_PUTS): detect put sell signals via supertrend
    # Phase 2 (SELL_CALLS): delegates to parent (OptionsStrategyBasic)
```

<!-- Phase 22-03 SUMMARY established patterns -->
Key patterns from 22-03-SUMMARY:
- Callable-based datasource: pass feature_vector_fn callable for state-dependent signal extraction
- V2 imports shared types (OptionContract, CloseSignalV2, etc.) from V1 to avoid duplication
- WheelStrategyV2 extends OptionsStrategyBasicV2 (not V1 parent)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CoveredCall datasource + V2 strategy + diff test</name>
  <files>
    src/clients/python/datasources/covered_call_signals.py
    src/clients/python/strategies/covered_call_v2.py
    src/clients/python/tests/test_covered_call_diff.py
  </files>
  <read_first>
    src/clients/python/strategies/covered_call.py
    src/clients/python/datasources/credit_spread_signals.py
    src/clients/python/strategies/credit_spread_v2.py
    src/clients/python/tests/test_credit_spread_diff.py
    src/clients/python/strategies/base_strategy.py
    src/clients/python/datasources/__init__.py
  </read_first>
  <action>
    1. Create `src/clients/python/datasources/covered_call_signals.py`:
       - Extract covered call signal detection from `OptionsStrategyBasic.check_for_new_signal()` (covered_call.py ~line 340+)
       - Function signature: `produce_open_signals(feature_vector_fn, ...)` following the callable-based datasource pattern from Phase 22-03 SUMMARY
       - The CoveredCall uses supertrend + stochrsi indicators on a candles_ltf DataFrame — the feature_vector_fn callable encapsulates the DataFrame access
       - Return list of signal dicts matching the datasource pattern
       - Per D-01: follow the exact Phase 21 extract-datasource pattern

    2. Create `src/clients/python/strategies/covered_call_v2.py`:
       - Class `OptionsStrategyBasicV2` extending `BaseStrategy` (per D-01 full copy approach, not inheritance from V1)
       - Import `from datasources.covered_call_signals import produce_open_signals`
       - Import shared types from V1: `from strategies.covered_call import OptionContract, OptionContractRepository, CloseSignalV2, OpenSignalV4, RollSignalV1, calculate_expected_profit_binomial_american, calculate_stock_quantity, generate_signal_stats`
       - Full copy of V1 logic, replacing inline signal detection with datasource call
       - label = "covered_call"
       - Copy ALL V1 behavior: the options ladder fetching, order placement, close/roll logic — only the signal DETECTION changes

    3. Create `src/clients/python/tests/test_covered_call_diff.py`:
       - Follow test_credit_spread_diff.py pattern exactly
       - Import both V1 (OptionsStrategyBasic) and V2 (OptionsStrategyBasicV2)
       - Patch signal detection in both modules to return controlled signals
       - Compare at the check_for_new_signal boundary (the point where V1 and V2 diverge)
       - Assert identical signal detection results given identical inputs

    4. Update `src/clients/python/datasources/__init__.py` if needed to export new module.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/test_covered_call_diff.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `covered_call_signals.py` exists and contains `def produce_open_signals`
    - `covered_call_v2.py` exists and contains `class OptionsStrategyBasicV2`
    - `covered_call_v2.py` contains `from datasources.covered_call_signals import`
    - `test_covered_call_diff.py` has at least 2 test functions and all pass
    - V2 imports shared types from V1 (no duplication of OptionContract, CloseSignalV2, etc.)
  </acceptance_criteria>
  <done>OptionsStrategyBasicV2 delegates signal detection to covered_call_signals datasource; diff test proves V1/V2 equivalence on same mock inputs</done>
</task>

<task type="auto">
  <name>Task 2: Wheel datasource + V2 strategy + diff test</name>
  <files>
    src/clients/python/datasources/wheel_signals.py
    src/clients/python/strategies/wheel_v2.py
    src/clients/python/tests/test_wheel_diff.py
  </files>
  <read_first>
    src/clients/python/strategies/wheel.py
    src/clients/python/strategies/covered_call_v2.py
    src/clients/python/datasources/covered_call_signals.py
    src/clients/python/tests/test_covered_call_diff.py
    src/clients/python/strategies/base_strategy.py
  </read_first>
  <action>
    1. Create `src/clients/python/datasources/wheel_signals.py`:
       - Extract WheelStrategy's put sell signal detection logic
       - The Wheel detects put sell signals using supertrend indicators during Phase 1 (SELL_PUTS)
       - Function: `produce_open_signals(...)` — same callable-based pattern
       - Return list of signal dicts for put sell opportunities

    2. Create `src/clients/python/strategies/wheel_v2.py`:
       - Class `WheelStrategyV2(OptionsStrategyBasicV2)` per D-06 — extends the V2 parent, NOT V1
       - Import `from strategies.covered_call_v2 import OptionsStrategyBasicV2`
       - Import `from datasources.wheel_signals import produce_open_signals`
       - Import shared types from V1 wheel: `from strategies.wheel import WheelPhase, PutSellSignal`
       - Full copy of V1 wheel logic with datasource delegation for put signal detection
       - Phase 1 (SELL_PUTS): uses wheel_signals datasource
       - Phase 2 (SELL_CALLS): delegates to parent OptionsStrategyBasicV2 (same as V1 pattern)

    3. Create `src/clients/python/tests/test_wheel_diff.py`:
       - Follow test_credit_spread_diff.py pattern
       - Import both V1 (WheelStrategy) and V2 (WheelStrategyV2)
       - Patch signal detection in both to return controlled signals
       - Test both phases: put sell (Phase 1) and covered call (Phase 2, delegates to parent)
       - Assert identical signal detection at the boundary
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/test_wheel_diff.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `wheel_signals.py` exists and contains `def produce_open_signals`
    - `wheel_v2.py` exists and contains `class WheelStrategyV2(OptionsStrategyBasicV2)`
    - `wheel_v2.py` contains `from datasources.wheel_signals import`
    - `wheel_v2.py` contains `from strategies.covered_call_v2 import OptionsStrategyBasicV2`
    - `test_wheel_diff.py` has at least 2 test functions and all pass
  </acceptance_criteria>
  <done>WheelStrategyV2 extends OptionsStrategyBasicV2, delegates put signals to wheel_signals datasource; diff test proves V1/V2 equivalence</done>
</task>

</tasks>

<verification>
All 6 new files exist. Both diff test suites pass. V2 inheritance chain: WheelStrategyV2 -> OptionsStrategyBasicV2 -> BaseStrategy.
</verification>

<success_criteria>
- covered_call_v2.py and wheel_v2.py exist with correct class hierarchy
- Both datasource modules extract signal detection from V1 strategies
- Both diff tests pass proving zero behavioral drift
- No duplication of shared types (imported from V1)
</success_criteria>

<output>
After completion, create `.planning/phases/25-complete-strategy-migrations/25-01-SUMMARY.md`
</output>
