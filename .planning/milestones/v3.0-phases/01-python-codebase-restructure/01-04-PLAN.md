---
phase: 01-python-codebase-restructure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/clients/python/tests/test_mean_reversion_strategy.py
  - src/clients/python/tests/test_options_mean_reversion_strategy.py
  - src/clients/python/tests/test_mean_reversion_report.py
  - src/clients/python/tests/test_credit_spread_strategy.py
autonomous: true
requirements: [CONS-04]
gap_closure: true

must_haves:
  truths:
    - "All existing strategy tests pass with zero failures after consolidation"
    - "pytest tests/ --ignore=tests/test_demo_covered_call.py exits with code 0"
  artifacts:
    - path: "src/clients/python/tests/test_mean_reversion_strategy.py"
      provides: "Mean reversion strategy tests with corrected @patch paths"
      contains: "strategies.mean_reversion"
    - path: "src/clients/python/tests/test_options_mean_reversion_strategy.py"
      provides: "Options mean reversion strategy tests with corrected @patch paths"
      contains: "strategies.options_mean_reversion"
    - path: "src/clients/python/tests/test_mean_reversion_report.py"
      provides: "Mean reversion report tests with corrected @patch paths and updated column expectations"
      contains: "tools.mean_reversion_report"
    - path: "src/clients/python/tests/test_credit_spread_strategy.py"
      provides: "Credit spread strategy tests with corrected test setup"
  key_links:
    - from: "tests/test_mean_reversion_strategy.py"
      to: "strategies/mean_reversion.py"
      via: "@patch decorator strings"
      pattern: 'strategies\.mean_reversion\.'
    - from: "tests/test_options_mean_reversion_strategy.py"
      to: "strategies/options_mean_reversion.py"
      via: "@patch decorator strings"
      pattern: 'strategies\.options_mean_reversion\.'
    - from: "tests/test_mean_reversion_report.py"
      to: "tools/mean_reversion_report.py"
      via: "@patch decorator strings"
      pattern: 'tools\.mean_reversion_report\.'
---

<objective>
Fix 40 test failures caused by incomplete DIR-08 execution during the Python codebase restructure.

Purpose: Close the single verification gap blocking CONS-04 ("All existing strategy tests pass after consolidation"). The failures are in three categories: (1) 36 stale @patch() decorator strings still referencing old bare module names, (2) 1 test expecting a renamed column (`p_revert` -> `p_revert_unbounded`/`p_revert_bounded`), and (3) 1 test with a setup that no longer matches the strategy's spread width logic after consolidation, plus 3 tests in mean_reversion_report with stale @patch paths.

Output: All 336 tests pass (excluding test_demo_covered_call which requires a live server).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-python-codebase-restructure/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale @patch() paths and column assertion in test files</name>
  <files>
    src/clients/python/tests/test_mean_reversion_strategy.py
    src/clients/python/tests/test_options_mean_reversion_strategy.py
    src/clients/python/tests/test_mean_reversion_report.py
  </files>
  <read_first>
    src/clients/python/tests/test_mean_reversion_strategy.py
    src/clients/python/tests/test_options_mean_reversion_strategy.py
    src/clients/python/tests/test_mean_reversion_report.py
    src/clients/python/tools/mean_reversion_report.py (lines 98-106 for COLUMNS definition)
  </read_first>
  <action>
    1. In test_mean_reversion_strategy.py, replace ALL occurrences of @patch("mean_reversion_strategy. with @patch("strategies.mean_reversion. — there are 12 occurrences at lines 156, 157, 192, 207, 222, 223, 248, 249, 271, 272, 725, 737. This is a literal string replacement: every instance of the string `"mean_reversion_strategy.` inside @patch() decorators becomes `"strategies.mean_reversion.`.

    2. In test_options_mean_reversion_strategy.py, replace ALL occurrences of @patch("options_mean_reversion_strategy. with @patch("strategies.options_mean_reversion. — there are 7 occurrences at lines 202, 203, 237, 238, 271, 272, 301.

    3. In test_mean_reversion_report.py, replace ALL occurrences of @patch("mean_reversion_report. with @patch("tools.mean_reversion_report. — there are 3 occurrences at lines 451, 461, 474.

    4. In test_mean_reversion_report.py, fix test_report_columns_present (line 226-232): The expected_cols set contains "p_revert" but the actual COLUMNS list (tools/mean_reversion_report.py line 98-106) produces "p_revert_unbounded" and "p_revert_bounded" instead. Update the expected_cols set to replace "p_revert" with "p_revert_unbounded", "p_revert_bounded". Also add "requested_price", "exit_tier", "expected_pl_bucket", "duration_min" if they are in COLUMNS but missing from expected_cols, OR simply update the set to match what COLUMNS actually produces. The test should verify the columns that actually exist.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/test_mean_reversion_strategy.py tests/test_options_mean_reversion_strategy.py tests/test_mean_reversion_report.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"mean_reversion_strategy\.' tests/test_mean_reversion_strategy.py returns 0
    - grep -c '"options_mean_reversion_strategy\.' tests/test_options_mean_reversion_strategy.py returns 0
    - grep -c '"mean_reversion_report\.' tests/test_mean_reversion_report.py returns 0
    - All tests in these 3 files pass (0 failures)
  </acceptance_criteria>
  <done>All 39 @patch-related test failures are fixed. The 3 test files pass with zero failures.</done>
</task>

<task type="auto">
  <name>Task 2: Fix credit spread test setup for test_partial_leg_failure_funnel_counter</name>
  <files>
    src/clients/python/tests/test_credit_spread_strategy.py
  </files>
  <read_first>
    src/clients/python/tests/test_credit_spread_strategy.py (lines 1195-1250 for TestPartialLegFailure class)
    src/clients/python/strategies/credit_spread.py (lines 1054-1115 for _open_put_spread method, especially target_long_strike computation)
    src/clients/python/strategies/credit_spread.py (lines 622-710 for _check_entries and _place_entry flow)
  </read_first>
  <action>
    The test test_partial_leg_failure_funnel_counter (line 1221) expects entries_skipped_leg_failure==1 but the strategy short-circuits at entries_skipped_no_long because the long_put at strike=94 doesn't match the computed target_long_strike.

    The logic: target_long_strike = short_contract.strike - target_width. The short is at 99.0. The long is at 94.0, so for the long to match, target_width must be 5.0 (99 - 5 = 94). But the dynamic width computed by _compute_dynamic_spread_width() returns something different, causing the long candidate to fall outside tolerance.

    Fix approach — update the test setup so the long_put strike actually matches what the strategy computes as target_long_strike:

    a. Read _compute_dynamic_spread_width() to understand what width the test's setup would produce, OR read what max_spread_width defaults to in CreditSpreadStrategy.__init__.
    b. Then set the long_put strike = short_strike - computed_width = 99.0 - width. For example, if max_spread_width defaults to 5.0 and tolerance is 0.50, set long_put strike to 94.0 (which should work). If the default width is different (e.g., 3.0), set long_put strike to 96.0.
    c. The companion test test_partial_leg_failure_rollback (line 1195) uses the same strike setup and does NOT assert on funnel counters, so update it consistently.
    d. If fixing the strike alone doesn't resolve it, mock _compute_dynamic_spread_width to return a value that makes target_long_strike match the long_put strike in the test fixture.

    The goal: the test must reach the place_order calls where the second call raises an exception, triggering entries_skipped_leg_failure. The test must NOT short-circuit at entries_skipped_no_long.
  </action>
  <verify>
    <automated>cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/test_credit_spread_strategy.py::TestPartialLegFailure -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_credit_spread_strategy.py::TestPartialLegFailure passes both tests (test_partial_leg_failure_rollback and test_partial_leg_failure_funnel_counter)
    - pytest tests/test_credit_spread_strategy.py passes all tests with 0 failures
  </acceptance_criteria>
  <done>The credit spread test_partial_leg_failure_funnel_counter test passes. The test correctly exercises the leg failure path and verifies the funnel counter increments.</done>
</task>

</tasks>

<verification>
Run the full test suite (excluding the live-server-dependent demo test):

```bash
cd /Users/jamal/projects/slack-trading/src/clients/python && /Users/jamal/miniconda3/envs/grodt/bin/python -m pytest tests/ --ignore=tests/test_demo_covered_call.py -q
```

Expected: All tests pass (0 failures). This satisfies CONS-04.
</verification>

<success_criteria>
- All 40 previously failing tests now pass
- Zero test regressions (no previously passing tests broken)
- pytest tests/ --ignore=tests/test_demo_covered_call.py exits with code 0
- CONS-04 requirement is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-python-codebase-restructure/01-04-SUMMARY.md`
</output>
