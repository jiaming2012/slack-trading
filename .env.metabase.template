# .env.metabase.template -- copy to .env.metabase and fill in values
# Usage: cp .env.metabase.template .env.metabase

# Metabase application database (on DO Postgres)
MB_DB_TYPE=postgres
MB_DB_DBNAME=metabaseappdb
MB_DB_PORT=5432
MB_DB_USER=grodt
MB_DB_PASS=CHANGEME_POSTGRES_PASSWORD
MB_DB_HOST=159.89.226.131

# JVM memory cap (per D-01/INFRA-03 -- local Docker Desktop)
JAVA_OPTS=-Xmx768m -Xms256m

# Metabase port (internal)
MB_JETTY_PORT=3000
