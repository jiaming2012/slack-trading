[bumpversion]
current_version = 1.0.3
commit = True
tag = True
tag_name = base-image-{new_version}  # Use a unique prefix for the base image tag

[bumpversion:file:Dockerfile.base]
search = version="{current_version}"
replace = version="{new_version}"
