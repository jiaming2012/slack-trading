[bumpversion]
current_version = 1.0.0
commit = True
tag = True
tag_name = base-image-2-{new_version}  # Use a unique prefix for the base image 2 tag

[bumpversion:file:Dockerfile.base2]
search = version="{current_version}"
replace = version="{new_version}"